# KubeOnCall 代码风格重构计划

## 1. 文档目的

本文用于统筹 KubeOnCall 全项目的代码风格与工程结构重构。重构范围不只包含缩进、命名和 import 排序，还包括依赖注入方式、包结构、类职责、领域模型、异常处理、日志、测试风格以及工程门禁。

本轮重构遵循以下原则：

- 保持现有业务行为和公开 API 向后兼容。
- 先建立测试、格式化和静态检查基线，再进行结构调整。
- 机械格式化与逻辑重构分开提交，便于审查和回滚。
- 按业务域渐进重构，不一次性迁移全部源码。
- 每个阶段必须具备独立的验证和验收标准。
- 不清理或回滚与本计划无关的用户改动。

## 实施状态

更新时间：2026-07-13。

已完成的改造：

- 添加 `.editorconfig`、`.gitattributes`、Spotless、Checkstyle 和 Maven Enforcer。
- CI 改为使用 Maven Wrapper 执行 `verify`。
- 移除未实际使用的 Lombok 依赖和注解处理器。
- 对全量生产与测试 Java 文件执行统一格式化；同时清理 Markdown、YAML 等文本文件的尾随空白。
- 新增 `CONTRIBUTING.md`，固化本地格式化、验证和集成测试命令。
- 已执行 `./mvnw verify`：231 tests、0 failures、0 errors、0 skipped；Spotless、Checkstyle、ArchUnit 与打包均通过。
- 已移除生产代码中的构造器 `@Autowired` 标记，统一使用单一构造器注入；测试改为显式装配依赖。
- 已收敛知识库、技能、内存、问答、告警恢复与告警工作流等模块的测试专用重载构造器和可空依赖分支。
- 告警恢复已拆出恢复窗口解析与审计记录组件；内存服务已将检索、时间归一化、审计等依赖设为必需项。
- 已新增 `AskContextLifecycle`，将问答会话、记忆、技能激活及上下文压缩从 `AskService` 分离出来。
- 已新增 `AlarmEventPreparationService`，将告警指纹补全、策略评估与活动状态记录从 `AlertWorkflowService` 分离出来。
- 已新增 `AlertWorkflowRunner`，将模板选择、节点超时和依赖跳过处理从 `AlertWorkflowService` 分离出来。
- 已新增 `AlarmNodeNoiseSuppression` 与 `AlarmWorkflowEscalation`，将节点噪声抑制和升级判定的 Redis 状态处理从 `AlertWorkflowService` 分离出来。
- 已新增 `AlertWorkflowMemory`，将告警记忆召回、上下文注入、抽取与 fail-open 记录从 `AlertWorkflowService` 分离出来。
- 已新增 `AlarmWorkflowAuditRecorder`，将告警执行摘要、metadata、active/recovery payload 与审计写入从 `AlertWorkflowService` 分离出来，并补齐组件级单测。
- 已新增 `MemoryDocumentMapper`，将长期记忆与知识文档的转换、软删除和清理资格判定从 `MemoryService` 分离出来。
- 已新增 `MemoryOperationObserver`，将记忆指标与审计的 fail-open 写入从 `MemoryService` 分离出来。
- 已新增 `OperationAuditRecordFactory`，统一内存、知识与技能操作的审计记录构造。
- 已将 `KubeOnCallProperties` 的 Agent、Approval、RAG、Storage、MCP、Workflow、Audit、Integration、Alarm、Memory 与 Skill 配置域拆为独立类型，并保留原有 `kubeoncall.*` 配置键及嵌套类型兼容 API；新增 Binder 级配置绑定测试。
- 已新增 `PlannerContextAssembler`，将 Planner 请求上下文、知识合并、Skill 注入与工具证据收集从 `PlannerThinkNode` 分离出来。
- 已新增 `PlannerRuleEngine`，将 Planner 的意图、目标、参数、风险、缺失信号和任务构造规则从 `PlannerThinkNode` 分离出来，并补齐规则级单测。
- 已新增 `PlannerParameterResolver` 与 `PlannerTaskFactory`，将 Planner 参数推断、来源追踪和任务/SOP 构造从 `PlannerRuleEngine` 分离出来。
- 已新增 `ExecutorPlanFactory`，将 Executor 的执行器映射、参数补全、参数来源、执行摘要与 payload 构造从 `ExecutorThinkNode` 分离出来。
- 已新增 `GraphAuditMetadataFactory`，将 GraphState 审计 metadata 与重试计数解析从 `ExecutionAuditService` 分离出来。
- 已新增 `ExecutionAuditStatistics`，将执行审计统计聚合从 `ExecutionAuditService` 分离出来。
- 已接入 ArchUnit，自动保护核心业务域不反向依赖 `web`，并禁止 `web` 直接依赖 Redis、Elasticsearch、MinIO 客户端。
- 已新增领域入口契约 `AlarmIngress`，使 `AlarmNormalizer` 与 Web DTO 解耦，同时保持既有告警 JSON 请求兼容。
- 静默降级的审计、技能状态刷新、会话写入、记忆合并、告警状态反序列化、Rerank 与 Planner LLM 回退现会记录带上下文的日志。

阶段 3 的依赖注入统一已完成；Planner、Executor 与告警工作流的主要职责拆分已完成并通过全量门禁。阶段 5 仍需继续收敛告警、工具检索和索引热点；阶段 6 至阶段 8 的统一错误协议、稳定 DTO 与真实依赖集成测试仍待推进。结构性改动持续与全量机械格式化分开处理。

## 2. 当前工程盘点

### 2.1 项目规模

当前项目是 Java 17、Spring Boot 3.3.4、Maven 单模块后端：

- 生产 Java 文件：251 个。
- 测试 Java 文件：78 个。
- 主要业务域：Agent、Alarm、Approval、RAG、Memory、Skill、Tool、Workflow。
- Java record 使用较多，DTO 和不可变值对象已有较好的基础。
- 当前分支：`agent/rag-architecture-refactor`。
- 当前工作区包含本轮格式化和结构重构改动；格式化与结构改动持续分开维护。

现存 Surefire 报告记录：

- Tests：231。
- Failures：0。
- Errors：0。
- Skipped：0。

当前 `./mvnw verify` 已通过（231 个测试，零失败、零错误、零跳过），并完成格式、Checkstyle、ArchUnit、编译和打包验证。真实依赖集成测试仍需在 Docker Compose 环境中单独执行。

### 2.2 主要代码风格问题

1. 项目没有统一的 `.editorconfig`、Java 格式化插件和静态检查门禁。
2. import 顺序、换行、参数缩进和注释格式存在差异。
3. 约 20 个生产类显式标注 `@Autowired` 构造器，部分服务通过多组重载构造器支持可选依赖或测试装配。
4. 部分组件以 `null` 表示依赖未启用，增加运行时分支和空指针风险。
5. DTO 大量使用 record，但部分稳定返回结构仍使用无约束的 `Map<String, Object>`。
6. 异常处理策略不统一，存在直接捕获 `Exception`、忽略异常或仅依赖注释解释降级行为的情况。
7. 日志声明和关键上下文字段没有形成统一规范。
8. 包结构同时采用技术分层和业务域分层，`web/service/domain` 与 `alarm/rag/memory` 等业务包混杂。
9. 配置集中在单个超大类中，修改一个配置域容易影响其他配置域。
10. 项目架构文档存在状态漂移，例如文档仍描述缺少 Dockerfile、Helm 和 CI，但对应资产已经存在。

### 2.3 超大类与优先热点

当前达到或超过 300 行的生产类共 3 个：

| 类 | 行数（约） | 主要问题 |
| --- | ---: | --- |
| `AlertWorkflowService` | 490 | 主服务已移出事件准备、节点运行、记忆、降噪、升级和审计，仍负责告警全流程编排 |
| `PlannerQueryToolNode` | 327 | 工具检索、候选裁剪与结果组装混合 |
| `KnowledgeIndexAdmin` | 300 | 索引初始化、别名和 mapping 管理仍需进一步收敛 |

行数只用于识别热点，不作为唯一质量指标。最终以职责是否单一、依赖方向是否清晰、测试是否容易为准。

## 3. 统一代码风格目标

### 3.1 Java 基础规范

- 使用 UTF-8 和 LF 换行，文件末尾保留一个换行符。
- 使用 4 空格缩进，不使用 Tab。
- import 自动排序，禁止通配符 import 和无效 import。
- 单构造器 Spring Bean 不标注 `@Autowired`。
- 默认使用构造器注入，禁止新增字段注入和 setter 注入。
- 生产类型只保留一个明确的依赖构造入口。
- DTO、事件和值对象优先使用 record 和不可变集合。
- 不为了减少几行代码混用 Lombok、record 与手写访问器。
- 方法名使用动词表达行为，布尔方法使用 `is/has/can/should` 前缀。
- 避免含义不清的 `data/info/result/process/handle`，需要结合业务语义命名。
- 公共方法只在契约或非显然行为需要说明时编写 Javadoc，避免重复代码含义的注释。
- 注释重点解释约束、降级理由和业务决策，不解释语句本身。

### 3.2 空值和集合规范

- 返回集合时使用空集合，不返回 `null`。
- Spring 必需依赖不允许为 `null`。
- 可选组件优先使用 No-op 实现、条件 Bean 或明确的提供器，不通过构造器传入 `null`。
- `Optional` 主要用于方法返回值，不作为通用字段或方法参数替代品。
- record 构造器需要在边界处完成默认值、防御性复制或合法性校验。

### 3.3 异常和降级规范

- 不新增无说明的 `catch (Exception ignored)`。
- 业务异常、参数错误、依赖故障和系统错误使用不同异常类型或错误码。
- 明确每个外部依赖是 fail-open 还是 fail-closed，并在代码、指标和文档中保持一致。
- 被降级处理的异常至少留下日志、metrics 或 audit 中的一种可观测证据。
- Controller 不直接处理底层 Redis、Elasticsearch、MinIO 或 HTTP 客户端异常。
- 建立统一的 API 错误响应，但迁移期间保持现有成功响应兼容。

### 3.4 日志规范

- 统一使用 SLF4J。
- 日志消息保持稳定，动态值使用参数占位符。
- 关键链路按场景携带 `executionId`、`sessionId`、`fingerprint`、`policyId`、`skillId` 等上下文。
- 不记录 API key、凭据、完整 prompt、敏感告警标签或用户隐私信息。
- 预期内降级使用 `warn` 或指标；真正导致请求失败的异常使用 `error`。

### 3.5 测试规范

- 测试名清楚表达场景、动作和预期结果。
- 单元测试不依赖 Redis、Elasticsearch、MinIO 或外部 HTTP 服务。
- 集成测试使用 `*IT` 命名并通过 Maven profile 隔离。
- 复杂领域对象使用测试 fixture 或 builder，避免测试直接调用生产类的多组兼容构造器。
- Controller 使用 JSON 契约测试保护 API 兼容。
- Repository 和外部 adapter 通过集成测试验证真实协议。

## 4. 分阶段实施计划

### 阶段 0：建立不可破坏的基线

目标：冻结现有行为，为后续重构提供回归保障。

任务：

1. 配置 JDK 17，并重新执行 `./mvnw test`。
2. 记录测试数量、耗时和失败情况。
3. 为以下公开入口补充契约测试：
   - `/api/ask`
   - `/api/alarms`
   - `/api/knowledge`
   - `/api/memory`
   - `/api/skills`
   - `/api/approvals`
4. 固化关键兼容边界：
   - 请求与响应 JSON。
   - `application*.yml` 配置键。
   - Redis key 前缀和 TTL 语义。
   - Elasticsearch index、alias 和 mapping。
   - Skill frontmatter 和 Runbook metadata。
5. 生成包依赖和热点类清单。

验收标准：

- 默认测试全部通过。
- 公开 API 的关键 JSON 结构有自动化测试保护。
- 后续阶段能够明确判断是否发生行为变化。

### 阶段 1：建立格式化和静态检查门禁

目标：让代码风格从人工约定变成自动执行规则。

任务：

1. 添加 `.editorconfig`。
2. 在 Maven 中接入 Spotless，统一 Java 格式和 import 顺序。
3. 接入 Checkstyle，首批只启用稳定、低争议规则：
   - 禁止通配符 import。
   - 禁止无效 import。
   - 类型和成员命名检查。
   - 空代码块检查。
   - 文件换行和基础排版检查。
4. 接入 Maven Enforcer，固定 Java 17 和 Maven 最低版本。
5. CI 增加格式检查和静态检查步骤。
6. Java、YAML、Markdown、Helm 和 Grafana JSON 分别设置处理范围，避免错误格式化模板或生成资产。

验收标准：

- 本地具备统一的格式化和检查命令。
- CI 能拒绝新增的不合规代码。
- 所有规则均有清晰的修复方式，不一次性引入大量高争议告警。

### 阶段 2：全项目机械格式统一

目标：只统一表面风格，不调整业务结构和行为。

任务：

1. 全量格式化生产和测试 Java 源码。
2. 统一 import 顺序。
3. 清理无用 import、重复修饰符和明显无效代码。
4. 统一参数换行、链式调用、空行和注释排版。
5. 对 YAML、Markdown 和部署文件执行安全的基础格式检查。
6. 将机械格式化作为单独提交，不混入重命名、包迁移或逻辑修改。

验收标准：

- Spotless 和 Checkstyle 全部通过。
- 格式化前后单元测试结果一致。
- 公开 API、配置键和持久化结构没有变化。

### 阶段 3：统一依赖注入和对象构造

目标：消除多构造器、可空组件和测试装配对生产代码的侵入。

任务：

1. Spring Bean 统一使用单一构造器注入。
2. 单构造器移除显式 `@Autowired`。
3. 用 No-op 实现、条件 Bean 或 `ObjectProvider` 表达可选能力。
4. 清理构造器中的 `dependency == null` 默认逻辑。
5. 建立测试 fixture/builder，移除仅为测试保留的生产构造器。
6. 评估 Lombok 实际使用情况；若没有有效使用，从 `pom.xml` 删除依赖。
7. 构造依赖过多的类列入下一阶段拆分范围。

优先处理：

- `AlertWorkflowService`
- `KnowledgeIngestService`
- `MemoryService`
- `SkillController`
- RAG facade 和 repository 实现

验收标准：

- 生产 Bean 原则上只有一个依赖构造入口。
- 不以 `null` 表示 Spring 组件未启用。
- 测试不依赖生产类的测试专用构造器。

### 阶段 4：收敛包结构和模块边界

目标：以业务域为主组织代码，并固定依赖方向。

建议目标结构：

```text
com.kubeoncall
├── agent
│   ├── api
│   ├── application
│   ├── domain
│   └── infrastructure
├── alarm
├── rag
├── memory
├── skill
├── approval
├── workflow
├── tool
└── shared
    ├── config
    ├── error
    └── observability
```

任务：

1. 将 `web/dto` 中的 DTO 逐步迁回所属业务域的 `api` 包。
2. 将顶层 `domain` 中的对象迁回 Alarm、RAG、Agent、Approval 等业务域。
3. Repository 接口放在业务边界，实现放入 `infrastructure`。
4. Controller 只依赖 application service，不直接依赖存储实现。
5. `shared/common` 只保留真正跨域的能力，禁止成为杂物包。
6. 引入 ArchUnit 固化以下依赖约束：
   - Domain 不依赖 Web、Spring MVC 和基础设施实现。
   - API 不直接依赖 Redis、Elasticsearch、MinIO 客户端。
   - 业务域之间通过明确的 facade 或接口协作。
7. 分业务域迁移，不在一次提交中移动全部文件。

验收标准：

- 核心业务域具有清晰的 API、应用、领域和基础设施边界。
- ArchUnit 自动阻止反向依赖。
- 包迁移不改变公开 JSON 和 Spring 配置键。

### 阶段 5：拆解超大类

目标：降低单类复杂度，让主要业务流程可以独立测试和演进。

#### 5.1 拆分 `KubeOnCallProperties`

- 按 Agent、Approval、RAG、Storage、Workflow、Audit、Integration、Alarm、Memory、Skill 拆成独立配置类型。
- 保持现有 `kubeoncall.*` 配置键不变。
- 为每个配置域增加绑定和默认值测试。

#### 5.2 拆分 `AlertWorkflowService`

建议拆出：

- `AlarmEventPreparationService`
- `AlarmGovernanceService`
- `AlarmSuppressionCoordinator`
- `AlarmRecoveryCoordinator`
- `AlertWorkflowRunner`
- `AlarmWorkflowAuditRecorder`

主服务只负责按顺序编排这些能力。

#### 5.3 拆分 `AskService`

建议拆出：

- 会话上下文加载与保存。
- Memory 注入与提取调度。
- Skill 激活。
- Agent pipeline 执行。
- Ask 响应组装。
- Ask 审计生命周期。

#### 5.4 拆分 Agent Think Node

Planner 和 Executor 节点分别拆出：

- prompt 构建器。
- LLM client 调用。
- 响应解析器。
- 决策校验器。
- fallback 策略。

#### 5.5 拆分 Memory 和 Audit 服务

- `MemoryService` 分离搜索、清理、恢复和持久化操作。
- `ExecutionAuditService` 使用不同业务域的 audit factory 或 metadata builder。
- 消除多个审计方法中重复的 record 和 metadata 构建逻辑。

验收标准：

- 应用服务尽量控制在 300 行以内。
- 核心方法保持单一职责。
- 拆出的组件可以独立单测。
- 主编排类不直接处理存储细节和复杂字段拼装。

### 阶段 6：统一领域模型和 API 协议

目标：减少无约束 Map 和隐式约定，提高类型安全。

任务：

1. 将稳定返回结构从 `Map<String, Object>` 转成命名 record。
2. metadata、diagnostics 等天然动态字段允许保留 Map，但集中构建和校验。
3. 统一时间类型为 `Instant`/`Duration` 等明确语义类型。
4. 状态统一使用领域枚举，减少散落的状态字符串。
5. record 构造时执行默认值、防御性复制和合法性校验。
6. 建立统一 `ApiError` 和全局异常处理器。
7. 区分参数错误、业务拒绝、审批暂停、外部依赖故障和系统异常。
8. 通过兼容字段或版本化方式渐进迁移错误响应。

验收标准：

- 稳定业务协议有明确类型。
- Controller 不拼装复杂业务 Map。
- 错误响应可预测、可审计，并保留兼容策略。

### 阶段 7：统一日志、异常和降级策略

目标：让故障能够被定位，并保持各模块降级语义一致。

任务：

1. 统一日志声明和消息格式。
2. 统一关键链路上下文字段。
3. 盘点所有 `catch (Exception)`、`ignored` 和 fallback 路径。
4. 为每条降级路径标明 fail-open 或 fail-closed。
5. HTTP、Redis、Elasticsearch 和 MinIO 异常统一转换为应用异常。
6. 统一超时、重试和错误码。
7. 为重要降级补 metrics 和 audit。

验收标准：

- 不存在无记录、无指标、无审计依据的关键异常吞噬。
- 相同类型外部故障具有一致的错误语义。
- 日志不泄露凭据、prompt 或敏感业务数据。

### 阶段 8：统一测试结构和质量门禁

目标：让测试结构与生产模块结构保持一致。

任务：

1. 测试包与生产包对齐。
2. 统一测试命名和 Given/When/Then 结构。
3. 建立 Alarm、RAG、Memory、Skill、Agent 公共 fixture。
4. 将单元测试和真实依赖集成测试严格分层。
5. 补充 Redis、Elasticsearch、MinIO 和 HTTP adapter 集成测试。
6. 增加 API JSON 契约测试。
7. 增加 ArchUnit 架构测试。
8. CI 顺序统一为：格式检查、静态检查、单元测试、打包、可选集成测试。

验收标准：

- 默认测试不依赖外部服务。
- 集成测试可以通过明确 profile 执行。
- 关键 API、包边界和配置绑定均有自动化保护。

### 阶段 9：文档和仓库结构收尾

目标：让工程文档反映当前真实实现。

任务：

1. 更新 `项目架构.md` 中已经过期的 Docker、Helm、CI 和资源描述。
2. 同步 `重构计划/当前重构进度.md`。
3. 合并重复或已经完成的专项重构计划。
4. 将面试资料与生产工程文档明确分区。
5. 新增贡献指南，包含：
   - JDK 和 Maven 要求。
   - 格式化命令。
   - 单元测试和集成测试命令。
   - 新增业务域、配置、DTO 和异常的规范。
6. 确保运行手册、部署模板和当前代码一致。

验收标准：

- 新成员可以只依据仓库文档完成开发环境配置、格式化、测试和提交。
- 架构文档不再描述已经不存在的工程状态。

## 5. 推荐实施与提交顺序

建议按以下独立批次执行：

1. `chore(baseline)`：恢复 JDK 17，固定测试与 API 契约基线。
2. `build(style)`：添加 EditorConfig、Spotless、Checkstyle、Enforcer 和 CI 门禁。
3. `style(java)`：全量机械格式化，不包含逻辑修改。
4. `refactor(di)`：统一构造器注入、可选组件和测试 fixture。
5. `refactor(config)`：拆分 `KubeOnCallProperties`。
6. `refactor(alarm)`：拆分 `AlertWorkflowService` 和恢复/抑制协调逻辑。
7. `refactor(ask)`：拆分 `AskService` 和 Agent Think Node。
8. `refactor(memory-rag)`：收敛 Memory、RAG 和 Audit 服务。
9. `refactor(packages)`：按业务域调整包结构并添加 ArchUnit。
10. `refactor(api)`：统一类型、错误协议、日志和降级行为。
11. `test(style)`：统一测试结构并补充集成测试。
12. `docs(style)`：更新架构、运行手册和贡献指南。

禁止将“全量格式化”“包迁移”“业务逻辑调整”放入同一个提交。

## 6. 每阶段统一验证清单

每个阶段至少执行：

```bash
./mvnw spotless:check
./mvnw checkstyle:check
./mvnw test
```

涉及集成能力时执行：

```bash
docker compose config
./mvnw -Pintegration-test verify
```

同时人工检查：

- 公开 API JSON 是否兼容。
- 配置键和默认值是否兼容。
- Redis key、TTL 和状态语义是否兼容。
- Elasticsearch mapping 和 alias 是否兼容。
- metrics 名称和 audit 字段是否兼容。
- 是否混入与当前阶段无关的文件变更。

## 7. 最终完成标准

满足以下条件后，整体代码风格重构才视为完成：

1. 格式化、静态检查、单元测试和 CI 全部通过。
2. 生产代码不存在字段注入和测试专用构造器。
3. 可选依赖不再通过 `null` 表达。
4. 核心业务域具有清晰且自动校验的依赖边界。
5. 超大编排类已经完成职责拆分。
6. 稳定业务协议使用明确类型，动态 Map 有统一边界。
7. 异常、日志、metrics 和 audit 形成一致的故障语义。
8. 默认单测与真实依赖集成测试明确分层。
9. 公开 API、配置和持久化语义保持兼容，或具备明确迁移方案。
10. 架构文档、运行手册和工程实际状态一致。

## 8. 第一批实施建议

第一批建议只实施阶段 0 至阶段 2：

1. 恢复 JDK 17 和 217 个测试基线。
2. 添加格式化、静态检查和 CI 门禁。
3. 完成一次纯机械格式统一。

这批工作风险低、覆盖全项目，并能为后续依赖注入、热点类拆分和包结构调整建立可靠基础。完成后再进入阶段 3，避免在没有自动门禁的情况下开始大规模结构重构。
