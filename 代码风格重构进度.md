# KubeOnCall 代码风格重构进度

> 更新时间：2026-07-13
> 当前状态：进行中。基础风格门禁、依赖注入统一、P0 热点拆分、API 协议收敛和关键外部故障语义收敛已完成；真实依赖集成执行受 Docker 镜像可用性阻塞。

## 1. 当前代码与验证基线

| 项目 | 当前结果 | 说明 |
| --- | ---: | --- |
| 生产 Java 文件 | 278 | 已完成全量格式统一，当前无 300 行以上生产类 |
| 测试 Java 文件 | 86 | 包含架构、组件、HTTP JSON 契约与 4 项 `*IT` 集成测试 |
| 最近一次全量 `./mvnw verify` | 244 tests | 0 failures、0 errors、0 skipped；Spotless、Checkstyle、ArchUnit、编译和打包均通过 |
| Compose、Helm 与 ES 集成校验 | 已通过 | `docker compose config --quiet`、`helm lint deploy/helm/kubeoncall`、真实 `ElasticsearchKnowledgeE2EIT` 均通过；Docker daemon 可用，但 Redis/MinIO 镜像尚未成功拉取，完整真实依赖 profile 尚待执行 |

说明：本轮完成告警工作流、Planner 工具查询、知识索引治理和 API 边界收敛后，已再次执行全量 `./mvnw verify`。所有后续结构性改动仍应在合并前重新执行全量门禁。

## 2. 已完成的工程规范建设

- 新增 `.editorconfig` 与 `.gitattributes`，统一 UTF-8、LF 换行、4 空格缩进和文件末尾换行。
- Maven 已接入 Spotless、Checkstyle 与 Maven Enforcer；CI 统一通过 Maven Wrapper 执行 `verify`。
- 全量生产与测试 Java 文件已经过格式化，Markdown、YAML 等文本的尾随空白已清理。
- 移除未实际使用的 Lombok 依赖和注解处理器。
- 新增 `CONTRIBUTING.md`，固化格式化、默认验证和集成测试命令。
- 生产 Bean 已统一为单一构造器注入，已清理显式构造器 `@Autowired`、测试专用重载构造器以及以 `null` 表示可选依赖的主要分支。
- 已清理无上下文的通用异常忽略；关键 fail-open 路径现在至少留下日志、指标或审计证据。
- `KubeOnCallMetricsService` 对未提供的指标注册表使用空 `CompositeMeterRegistry`；`YamlAlarmPolicyRepository` 的测试构造工厂已迁移到测试 fixture，生产 API 不再承载测试便利入口。
- `MemoryConsolidationService` 的 repository、配置、metrics、审计和时间归一化协作者均为必需依赖，指标记录不再通过 `null` 分支跳过。

## 3. 已完成的架构和职责拆分

### 配置与边界

- 将原本集中的 `KubeOnCallProperties` 按 Agent、Approval、RAG、Storage、MCP、Workflow、Audit、Integration、Alarm、Memory、Skill 等配置域拆分为独立类型。
- 保留既有 `kubeoncall.*` 配置键及嵌套类型兼容入口，并补齐 Binder 级配置绑定测试。
- 引入领域入口契约 `AlarmIngress`；`AlarmNormalizer` 不再依赖 Web DTO，既有告警 JSON 请求保持兼容。
- 引入 ArchUnit 规则：核心业务域不得反向依赖 `web`；`web` 不得直接依赖 Redis、Elasticsearch、MinIO 客户端。

### 问答、告警与记忆

- `AskService`：抽出 `AskContextLifecycle`，集中管理会话、记忆、技能激活和上下文压缩。
- `AlertWorkflowService`：已抽出 `AlarmEventPreparationService`、`AlertWorkflowRunner`、`AlertWorkflowMemory`、`AlarmNodeNoiseSuppression`、`AlarmWorkflowEscalation`、`AlarmWorkflowAuditRecorder`；本轮继续抽出 `AlertWorkflowPreflight`、`AlarmWorkflowRecoveryHandler`、`AlarmWorkflowSuppressionHandler`、`AlarmWorkflowDeduplicator` 与 `AlertWorkflowCoordinator`，从约 492 行降至约 80 行，并沿用既有服务级回归测试保护行为。
- `MemoryService`：抽出 `MemoryDocumentMapper` 处理文档转换、软删除和清理资格；抽出 `MemoryOperationObserver` 处理指标与审计的 fail-open 写入。

### Planner、Executor 与审计

- `PlannerThinkNode`：由约 533 行降至约 162 行，职责拆至 `PlannerContextAssembler` 和 `PlannerRuleEngine`。
- `PlannerRuleEngine`：继续拆出 `PlannerParameterResolver`（参数推断与来源追踪）和 `PlannerTaskFactory`（任务与 SOP 构造），当前约 281 行。
- `ExecutorThinkNode`：由约 327 行降至约 181 行，抽出 `ExecutorPlanFactory` 负责执行器映射、参数补全、摘要和 payload 构造。
- `ExecutionAuditService`：由约 302 行降至约 226 行，抽出 `GraphAuditMetadataFactory` 与 `ExecutionAuditStatistics`。
- 审计记录构造已收敛到 `OperationAuditRecordFactory`，统一内存、知识和技能操作的审计字段。
- `PlannerQueryToolNode`：由约 327 行降至 43 行，抽出 `PlannerToolCandidates`、`PlannerToolEvidenceCollector` 与 `PlannerToolQueryResultAssembler`；正常 MCP 结果、补充信号查询和回退行为均有单测。
- `KnowledgeIndexAdmin`：由约 300 行降至 80 行，抽出命名、mapping、alias、初始化和版本管理组件，并保留既有公开 facade/返回 record。

### API 协议、日志与测试

- 新增全局 `ApiExceptionHandler`、`ApiErrorCode`、`ApiErrorResponse`，覆盖参数错误、畸形请求、资源不存在、冲突、审批暂停、重规划、业务拒绝和未预期错误；500 响应不泄露内部异常文本。
- 状态、技能、工具目录、告警抑制、维护窗口和告警策略的稳定成功响应已改为命名 record，JSON 字段保持兼容；metadata、diagnostics 和外部工具载荷仍为显式动态 Map 边界。
- 新增 MockMvc 成功/错误 JSON 契约测试，以及 Redis、MinIO、Elasticsearch 与本地可控 HTTP 适配器四项 `*IT` 集成测试；本地可控 HTTP 与真实 Elasticsearch 已通过，Redis/MinIO 待镜像可用后执行。
- MinIO 写入失败只记录 bucket 与异常类型，返回稳定消息，避免把底层异常写入知识 metadata；工具 HTTP、RAG fallback、记忆注入、告警节点、Runbook 导入、维护窗口、告警抑制和记忆死信队列的降级路径也只向客户端、上下文或持久化 metadata 返回稳定提示或异常类型。
- 告警恢复调度、知识/策略审计、Skill reload、技能状态刷新与记忆审计的 fail-open 日志也统一保留操作上下文与异常类型，不记录完整异常文案或本机路径。

## 4. 当前热点与剩余工作

当前没有达到或超过 300 行的生产类；行数仅作为职责审查信号，不是单独的质量判定。

| 优先级 | 项目 | 当前状态 | 后续处理方向 |
| --- | --- | --- | --- |
| P1 | 外部故障语义 | 关键链路已收敛 | 新增 adapter 时继续审查 HTTP、Redis、Elasticsearch、MinIO 的异常转换、日志和 fail-open/fail-closed 策略 |
| P1 | 真实依赖验证 | 部分通过、环境阻塞 | 真实 Elasticsearch 已通过；Docker daemon 可用，但 Redis/MinIO 镜像尚未成功拉取，镜像可用后执行完整 `./mvnw -Pintegration-test verify` |
| P2 | 外部 HTTP adapter | 受控集成已通过 | `ToolHttpClientIT` 覆盖 JSON、非 JSON、认证头、非 2xx 和超时五种场景，稳定错误字段受保护 |
| P2 | 文档一致性 | 已更新本轮文档 | 交付、配置和运行方式变化时同步维护 |

## 5. 后续执行顺序

1. 确保 Docker 可拉取 Redis、MinIO 镜像，启动 Compose 的 Redis、Elasticsearch、MinIO，并执行完整 `./mvnw -Pintegration-test verify`。
2. 新增外部 HTTP 协议时同步扩展认证、超时与响应格式的集成契约。
3. 持续复查新增外部依赖的日志、异常降级、指标和审计语义。
4. 每次结构调整后执行 `./mvnw verify`，并同步更新交付文档。

## 6. 兼容性与验收约束

- 不修改既有公开 API 的请求 JSON、成功响应结构和关键配置键，除非先提供兼容迁移。
- 保持 Redis key/TTL、Elasticsearch 索引/alias/mapping、Skill frontmatter 与 Runbook metadata 的既有语义。
- 机械格式化与结构性改动继续分开维护，避免审查时混淆行为变化。
- 最终完成的判定以全量门禁、核心 JSON 契约测试和真实依赖集成验证为准，而非仅以文件行数下降为准。
