# KubeOnCall 全项目重构实施计划

## 1. 总览

本计划用于统筹 KubeOnCall 后端重构，目标是达到**后端可验收闭环**，采用**分阶段验收**方式推进。

固定实施顺序：

1. 工具链基线
2. 告警治理
3. 工作流模板化
4. RAG 基础重构
5. 记忆系统
6. Skill 底座
7. 全链路收敛

约束：

- 不引入 PostgreSQL/MySQL 等新存储。
- Redis 存短期状态、会话状态、active alarm 状态。
- Elasticsearch 存知识库与长期记忆。
- MinIO 存原始知识文档。
- RAG 向量能力首轮做接口与可测实现，真实 embedding/kNN 通过配置启用。
- 现有公开 API 必须保持向后兼容。
- 现有工作区中的无关删除和未跟踪文件不得回滚或清理。

## 1.1 当前进度同步

更新时间：2026-07-17 CST

当前代码与提交状态：

- 当前分支：`agent/rag-architecture-refactor`。
- 当前工作区 `./mvnw test` 已通过：313 tests，0 failures，0 errors，0 skipped；Spotless、Checkstyle 和 ArchUnit 通过。Docker Desktop 当前未启动，本轮未重跑真实依赖集成。
- Node Exporter → Prometheus → Alertmanager → KubeOnCall 已完成真实 `pending/firing/resolved`、Bearer、202、去重、Inbox、审计和通知 HTTP 调用验收。
- 阿里云百炼 `text-embedding-v4`、`qwen3-rerank`、`qwen-plus` 已完成鉴权与协议验证；应用级 ES kNN、cross-encoder 主排序、超时和维度错误降级审计已通过。
- `qwen-plus` 已通过记忆 pending/processing 队列、质量过滤、ES 落库、召回和软删除的完整真实流水线验收。
- 动态 MCP 已完成 `tools/list` → 只读工具选择 → 参数解析 → Bearer 调用 → Planner 证据回填的通用闭环，并通过本地真实 HTTP 协议验收。
- 业务 API viewer/operator/admin 鉴权、审批决策 CAS/恢复租约 heartbeat、告警 consumer fencing/生命周期预留/retry-dead-letter 原子迁移、记忆 claim fencing/heartbeat、RAG 请求限额/写入补偿/旧对象清理和 Skill 指名领域信号约束已完成。
- 工作区仍有用户已有的面试资料删除，后续不得回滚、清理或混入重构提交。

阶段状态：

| 阶段 | 当前状态 | 剩余边界 |
| --- | --- | --- |
| 阶段一：工具链与基线 | 已完成 | 依赖或协议变化后重跑门禁即可。 |
| 阶段二：告警治理 | 当前节点 MVP 已完成 | NodeNotReady、多集群和复杂生产治理明确延期。 |
| 阶段三：工作流 | 当前节点 MVP 已完成；告警 claim heartbeat/生命周期预留和审批恢复 lease heartbeat 已补 | PagerDuty/Jira/ServiceNow、工单审批和复杂升级不排期。 |
| 阶段四：RAG | 当前代码、真实模型联调、数据增强、批处理和旧 MinIO 对象清理已完成 | 离线效果评估和真实 alias 切换延期。 |
| 阶段五：记忆 | 代码层、真实 LLM、模型计数、语义去重、整理锁和提取 claim heartbeat 完成 | 质量评估集和负载验收延期。 |
| 阶段六：Skill | 核心底座、8 个生产样例、指名领域信号约束、标准 MCP JSON-RPC、本地 allowlist 和 OAuth client-credentials 完成 | 真实 OAuth token endpoint/MCP 服务现场验收延期。 |
| 阶段七：交付 | 本机 Docker/Prometheus/Alertmanager 已验收；Helm 完整参数、生产 values、Dashboard ConfigMap、CI/CD Provider 投递资产和集群验收脚本已补齐 | Minikube 当前未运行，Helm/Grafana 实际部署和外部通知渠道延期。 |

## 1.2 当前仍需要重构的事项

当前收紧计划没有必须继续开发的阻塞项。若恢复原始“完整生产平台”目标，代码层仍有以下 backlog：

| 建议顺序 | 模块 | 尚未开发或只部分实现 | 类型 |
| --- | --- | --- | --- |
| 1 | 效果评估 | 记忆质量集、RAG 离线评估和并发负载基线；按当前要求不实施 | 评估/压测缺口 |
| 2 | 外部联调 | 真实 tokenizer chat-usage、MCP OAuth token endpoint/生产服务和 CI/CD ChangeEvent Provider 的现场投递 | 环境缺口 |
| 3 | 告警核心测试 | `/api/alarms` 旧/新 JSON 全链路、Alertmanager + Redis 乱序/批量端到端、Redis session/approval/audit 真实集成、P1 持续时间升级和策略历史版本回放/回滚 | 自动化测试缺口，已重新纳入计划 |
| 4 | 接入边界测试 | ChangeEvent Provider 鉴权/映射/幂等、MCP OAuth 缓存/刷新/metadata discovery、外部通知与 Incident Adapter 完整链路 | 自动化与外部集成缺口 |

以下已有代码或资产，不应再写成“未开发”：业务 API 分级鉴权、审批 CAS/恢复租约 heartbeat、告警 Inbox consumer fencing/生命周期预留/原子迁移与通知幂等键、真实 embedding/rerank、批量 embedding、RAG 写入补偿/导入限额/旧 MinIO 对象清理、并发 RAG 双字段增强、model/version-dataset 绑定、真实 qwen 记忆流水线、自动校准 chat-usage token 计数、语义去重、lease 续租、claim fencing/heartbeat、`RetrievalHit` 与阶段 trace、alias/reindex API、Skill project override/disabled/reload/版本冲突、8 个生产 Skill、`applicableTasks/tags`、指名 Skill 领域信号约束、`load_skill`、带本地 allowlist/分页/ID/有效快照保留/读取阶段响应边界/增量 SSE/多层 Schema/OAuth metadata discovery 的动态 MCP、Redis Skill/告警策略快照、CI/CD ChangeEvent 全入口鉴权/Provider HMAC-token 投递/预读取 payload 边界/ExecutionAudit/事件级幂等、MTTA/MTTR/quality Grafana 面板、统一预算、Dockerfile、生产 values 与 Dashboard ConfigMap、CI、结构化 `AskResponse`、Prometheus rules。

以下属于环境验收或明确延期，不是代码缺口：真实 ES alias 切换演练、Helm 集群部署、Grafana 实际展示、钉钉/邮件/PagerDuty/Jira/ServiceNow、多集群和 NodeNotReady。

## 1.3 推荐实施顺序

当前默认停止点已经达到。只有用户明确恢复生产增强范围时，才按以下顺序继续：

1. 先补 `/api/alarms` Controller、Alertmanager + Redis、Redis session/approval/audit、P1 持续时间升级和策略版本生命周期五项核心自动化测试。
2. 再补 ChangeEvent Provider、MCP OAuth 和外部通知/Incident Adapter 的边界测试。
3. 执行真实 tokenizer chat-usage、生产 MCP OAuth、CI/CD ChangeEvent Provider、Helm/Grafana 和外部通知渠道的环境验收。
4. 记忆质量集、RAG 离线评估、并发负载、真实 ES alias 切换/回滚、复杂灰度和多集群测试继续延期。

后续第 2～11 节保留为原始完整方案和接口约束；判断当前状态时，以本节和 `当前重构进度.md` 为准，不再把历史任务列表直接视为待办。

## 2. 公开接口与类型调整

### 2.1 `/api/alarms`

保持旧格式兼容，`AlarmRequest` 增加标准字段：

- `fingerprint`
- `alertName`
- `resourceType`
- `resourceName`
- `cluster`
- `namespace`
- `service`
- `metricName`
- `currentValue`
- `threshold`
- `unit`
- `duration`
- `labels`
- `annotations`
- `runbookId`
- `status`

响应保持 `List<NodeResult>`。

新增领域类型：

- `AlarmSeverity`: `P0/P1/P2/P3/INFO`
- `AlarmStatus`: `FIRING/RESOLVED/SUPPRESSED`
- `AlarmResourceType`
- `NormalizedAlarmEvent`
- `AlarmEvaluationResult`

### 2.2 `/api/ask`

`AskRequest` 增加可选 `sessionId`。

`AskResponse` 返回 `sessionId`。

无 `sessionId` 时保持现有单轮行为。

### 2.3 `/api/knowledge/query`

`KnowledgeQueryRequest` 增加：

- `topK`
- `retrieveMethod`: `KEYWORD/VECTOR/HYBRID`
- `includeTrace`

默认保持兼容旧请求。

### 2.4 工具目录

新增安全读类能力：

- `kubernetes.describeResource`
- `alertmanager.sendAlertEvent`

`alertmanager.createSilence` 保留，但只能作为审批后的显式动作，不再作为告警结果默认动作。

## 3. 阶段一：工具链与基线

目标：让项目具备可重复验证入口。

任务：

1. 添加 Maven Wrapper。
2. 明确 JDK 17 要求。
3. 默认验证命令统一为：

```bash
./mvnw test
```

4. 建立默认单测与 Docker-backed 集成测试分层。
5. 添加测试样例：
   - 旧格式告警 JSON。
   - 新格式告警 JSON。

验收：

- 默认测试不依赖 Redis/ES/MinIO。
- 集成测试可在 `docker compose up -d redis elasticsearch minio` 后运行。
- 工具链问题不会阻塞后续阶段的单测编写。

## 4. 阶段二：告警治理主线

目标：把告警链路从“上游字符串 + 固定流程”升级为“标准事件 + 策略分级 + 可审计诊断”。

任务：

1. 实现 `AlarmNormalizer`。
   - 旧字段映射到 `NormalizedAlarmEvent`。
   - 从 `metadata` 提升 `namespace/service/cluster/alertName/runbookId` 等常用字段。
2. 实现 `AlarmFingerprintService`。
   - 优先使用 `fingerprint`。
   - 其次使用 `dedupKey`。
   - 缺失时使用 `alertName + cluster + namespace + resourceType + resourceName + service + metricName + sorted stable labels` 生成 SHA-256。
3. `AlertWorkflowService` 内部改用 normalized event 和 fingerprint 去重。
4. 保留 `process(AlarmEvent)` 兼容入口。
5. 实现 `YamlAlarmPolicyRepository` 与 `AlarmPolicyEngine`。
6. 首批策略覆盖：
   - CPU
   - 内存
   - 磁盘
   - inode
   - NodeNotReady
   - PodCrashLoop
   - OOMKilled
   - DeploymentUnavailable
7. `StateCompareNode` 使用策略中的 PromQL/window。
8. `ResultPushNode` 默认只输出结构化诊断 summary，不自动 `createSilence`。

验收：

- 旧格式请求仍能执行。
- 新格式请求生成稳定 fingerprint。
- 无 `dedupKey` 时不会产生 `alarm-dedup:null`。
- CPU 69 不触发策略，70.1 触发 P1，85.1 触发 P0。
- P0/P1 默认不创建 silence。

## 5. 阶段三：工作流模板、抑制与恢复

目标：不同告警走不同诊断路径，并具备噪声治理能力。

任务：

1. 新增 `WorkflowTemplateRegistry`。
2. `AlertWorkflowFactory.buildWorkflow(workflowTemplate)` 支持：
   - `host-resource`
   - `k8s-pod`
   - `k8s-node`
   - `workload`
   - `control-plane`
3. Redis 存 active alarm：
   - `firstSeen`
   - `lastSeen`
   - `count`
   - `status`
   - `severity`
   - `fingerprint`
4. 去重 TTL 按 severity/category 配置。
5. NodeNotReady 抑制同节点 Pod 噪声。
6. P0/P1 未确认升级。
7. 支持恢复事件确认。
8. 拆分安全节点：
   - `NotificationNode`
   - `TicketNode`
   - `SilenceNode`

验收：

- 不同 `workflowTemplate` 生成不同节点序列。
- 依赖失败时降级和审计正确。
- Silence 只走审批路径。

## 6. 阶段四：RAG 重构

目标：修复当前 RAG 的数据工程问题，让后续记忆和告警 Runbook 绑定有稳定基础。

任务：

1. `KnowledgeIngestService` 保留兼容 facade。
2. 内部拆分：
   - `KnowledgeIngestionFacade`
   - `KnowledgeRetrievalFacade`
3. 标准化 metadata：
   - `doc_id`
   - `chunk_id`
   - `chunk_index`
   - `total_chunks`
   - `parent_document_id`
   - `document_type`
   - `source_type`
   - `dataset_version`
   - `chunk_enable`
   - `file_hash`
4. 替换固定 280 字符 chunker 为配置化 Markdown/recursive chunker。
5. 保存 parent 文档。
6. 检索不再按 `createdAt` 覆盖相关性。
7. 抽象：
   - `EmbeddingClient`
   - `EmbeddingService`
   - `VectorRetriever`
8. 测试使用确定性 mock embedding。
9. ES kNN 通过配置启用。
10. Rerank 接收 retrieval hit 和 score trace，失败时回退 RRF。

验收：

- parent 可回源。
- chunk metadata 完整。
- 排序不被 createdAt 覆盖。
- vector disabled/mock/failed/native-kNN 代码路径可测；真实 ES mapping、写入、过滤和 KNN 已通过集成验收。

## 7. 阶段五：记忆系统

目标：补齐会话记忆、长期记忆和告警记忆。

任务：

1. `SessionStore` 使用 Redis。
2. `AskService` 支持多轮 session。
3. 无 `sessionId` 时仍单轮。
4. 新增：
   - `MemoryEntry`
   - `MemoryType`
   - `MemoryScope`
5. 长期记忆写入 ES，`source_type=memory`。
6. 知识检索默认不被 memory 污染。
7. `MemoryInjector` 只注入：
   - `SERVICE_FACT`
   - `KNOWN_PITFALL`
8. 注入内容必须带新鲜度和“先验证当前状态”提示。
9. `MemoryExtractor` 异步从 Ask/Alarm 结果中提取记忆。
10. 提取时排除：
    - kubectl 命令
    - 实时状态
    - SOP 全文
    - 可从监控/日志重新获得的内容
11. `AlertMemoryService` 按 fingerprint/service/resource 召回历史处置。

验收：

- 同一 session 可追问。
- 长期记忆按 `source_type=memory` 隔离。
- 陈旧提示存在。
- 自动提取失败不影响主流程。

## 8. 阶段六：Skill 底座

目标：让运维经验以可加载、可版本化的 Skill 形式沉淀。

任务：

1. 实现：
   - `Skill`
   - frontmatter parser
   - `SkillRegistry`
   - `SkillMatcher`
   - `SkillActivationService`
2. 第一版 Skill 只做自动匹配和 prompt 注入。
3. 不做真 tool-calling。
4. `PlannerLlmService` 注入 skill 索引和激活正文。
5. `AgentToolCatalog` 支持 tool whitelist。
6. `VerifierThinkNode` 读取 skill `maxRisk` 联动审批。
7. 添加示例 skill：
   - `payment-oom-triage`

验收：

- 无关请求不激活。
- OOM 请求激活示例 skill。
- 工具不在激活 Skill 白名单时返回 `SKILL_TOOL_NOT_ALLOWED`，禁止回退完整工具目录。

## 9. 阶段七：收敛与治理

目标：让重构后的后端具备可运维、可追踪、可交接能力。

任务：

1. 所有新增流程写入 `ExecutionAuditService`。
2. metrics 覆盖：
   - alarm 成功率/fallback
   - RAG 命中率/fallback
   - memory 提取与注入
   - skill 激活与白名单拒绝
3. 补充后端运行文档：
   - 本地启动
   - 测试
   - 策略配置
   - RAG 重建
   - 记忆清理
   - Skill 添加
4. 清理不再使用的旧内部路径。
5. 保留公开 API 的向后兼容字段。

验收：

- 全部默认单测通过。
- 核心链路可通过 API 样例验收。
- 文档足够让后续工程师继续维护。

## 10. 测试总清单

### 工具链

- `./mvnw test` 可作为默认测试入口。

### 告警

- 旧格式请求仍执行。
- 新格式请求生成稳定 fingerprint。
- CPU 69 不触发。
- CPU 70.1 触发 P1。
- CPU 85.1 触发 P0。
- 无 `dedupKey` 不产生 `alarm-dedup:null`。

### 安全

- P0/P1 默认不创建 silence。
- `createSilence` 只有审批后节点可调用。
- `/api/alarms/silence-approvals` 可写入短期审批，过期审批不会触发 silence。

### 工作流

- 不同 `workflowTemplate` 生成不同节点序列。
- 依赖失败时降级和审计正确。
- silence 审批信息会注入工作流上下文和 Alarm 审计 metadata。

### RAG

- parent 可回源。
- chunk metadata 完整。
- 排序不被 createdAt 覆盖。
- vector disabled/mock/failed/native-kNN 代码路径可测；真实 ES 验收由 `integration-test` profile 执行。

### 记忆

- 同一 session 可追问。
- 50 轮 session 压缩后保留早期关键事实和最近结论。
- planning context 和 observations 不超过配置预算。
- 长期记忆按 `source_type=memory` 隔离。
- memory 搜索复用 hybrid retrieval，并可返回 trace。
- cleanup/consolidation/restore 有独立 Memory 审计。
- 多实例定时整理由 Redis lease lock 互斥。
- 软删除记忆可通过恢复 API 回滚。
- 陈旧提示存在。
- 自动提取失败不影响主流程。

### Skill

- 无关请求不激活。
- OOM 请求激活示例 skill。
- 工具不在激活 Skill 白名单时 fail-closed，且不生成执行载荷。

## 11. 第一批建议 PR

1. `PR-01-tooling-baseline`
2. `PR-02-alarm-normalization`
3. `PR-03-alarm-policy-engine`
4. `PR-04-safe-workflow-output`
5. `PR-05-workflow-templates`
6. `PR-06-rag-metadata-chunk-parent`
7. `PR-07-rag-retrieval-trace-vector-interface`
8. `PR-08-session-memory`
9. `PR-09-longterm-alert-memory`
10. `PR-10-skill-foundation`
11. `PR-11-docs-audit-metrics-cleanup`
