# KubeOnCall 全项目重构实施计划

## 1. 总览

本计划用于统筹 KubeOnCall 后端重构，目标是达到**后端可验收闭环**，采用**分阶段验收**方式推进。

固定实施顺序：

1. 工具链基线
2. 告警治理
3. 工作流模板化
4. RAG 基础重构
5. 记忆系统
6. Skill 底座
7. 全链路收敛

约束：

- 不引入 PostgreSQL/MySQL 等新存储。
- Redis 存短期状态、会话状态、active alarm 状态。
- Elasticsearch 存知识库与长期记忆。
- MinIO 存原始知识文档。
- RAG 向量能力首轮做接口与可测实现，真实 embedding/kNN 通过配置启用。
- 现有公开 API 必须保持向后兼容。
- 现有工作区中的无关删除和未跟踪文件不得回滚或清理。

## 1.1 当前进度同步

更新时间：2026-07-10 18:59 CST

当前代码与提交状态：

- 最新已推送重构提交：`e5f3fcd refactor(memory): harden lifecycle governance`，当前分支 `agent/rag-architecture-refactor`。
- 工作区仍有一批用户已有的资料删除，后续重构不得回滚、清理或混入提交。
- 最近一次完整验证：`./mvnw -Pintegration-test verify` 通过；随后默认测试增至 158 个并再次通过；真实 Elasticsearch 集成测试 1 个，均为 0 failures、0 errors、0 skipped。
- 默认单测已通过 Mockito subclass mock maker 规避 JVM attach 依赖，非提权环境可直接执行。

阶段状态：

| 阶段 | 状态 | 已完成内容 | 未完成/下一步 |
| --- | --- | --- | --- |
| 阶段一：工具链与基线 | 已完成 | Maven Wrapper 已接入，JDK 17 测试入口统一；默认单测不依赖 Redis/ES/MinIO；新增 Maven Failsafe `integration-test` profile，真实 ES 测试与默认单测分层。 | 后续补 Redis/MinIO 等真实依赖 profile。 |
| 阶段二：告警治理主线 | 已完成 | 已实现标准告警类型、`AlarmNormalizer`、稳定 fingerprint、YAML 策略仓库、策略引擎、首批 CPU/内存/磁盘/inode/Node/Pod/Deployment 策略；`StateCompareNode` 使用策略 PromQL/window；`AlarmPolicy` 支持 `ragFilters`；`ResultPushNode` 默认不自动 silence；策略 `labels` 与 `matchLabels` 已分离，旧格式 critical 告警可按告警族和上游 severity 匹配且不会降为 P3。 | 后续进入策略生产治理：策略版本化、灰度、热加载、回放评估、维护窗口。 |
| 阶段三：工作流模板、抑制与恢复 | 代码闭环完成，生产系统联调待补 | 已实现 `WorkflowTemplateRegistry`、active alarm、分级去重、NodeNotReady 抑制、acknowledge API、稳定恢复状态机、P0/P1 人工确认、P2/P3 自动确认；通知调用 Alertmanager adapter，工单/Incident 创建、升级、恢复关闭和 P0/P1 复盘调用 Incident adapter；告警 RAG 已安全绑定 8 份生产 SOP 正文资产。 | 后续补主动健康检查、确认超时、具体 PagerDuty/Jira/ServiceNow 认证契约联调、维护窗口，以及工作流模板配置化或外部化。 |
| 阶段四：RAG 重构 | 架构主链路和真实 ES 验收完成 | 已完成 facade、metadata、parent、配置化 chunker、ES hit 顺序、embedding、native kNN、cross-encoder；metadata 使用 `flattened`；SOP 支持版本化幂等导入；真实 ES 已验收索引创建、mapping、父子写入、精确过滤、keyword、KNN、父文档聚合和重复导入。 | 待真实 HTTP embedding/cross-encoder 服务联调；仍缺 `RetrievalHit`、完整分数/阶段 latency trace、索引 alias/rebuild 和数据增强。 |
| 阶段五：记忆系统 | P1/P2 主链路和基础治理完成 | 已完成 Redis session、双层压缩、统一 token budget、50 轮关键事实保留测试、hybrid memory 检索/trace、安全 cleanup；`MemoryConsolidationJob` 支持定时、dry-run、重复软删除、存量相对日期标准化和 Redis lease lock；已提供软删除恢复 API 和独立 Memory 审计；`IntelligentDiagnosisNode` 已安全消费历史告警记忆并输出实时验证 guardrails。 | 缺 LLM 诊断增强和真实 Redis/ES 集成验收。 |
| 阶段六：Skill 底座 | 第一版完成，生产治理待补 | 已实现 `Skill`、frontmatter parser、`SkillRegistry`、`SkillMatcher`、`SkillActivationService`；`AskService` 会自动激活 Skill 并把 prompt、工具白名单、`maxRisk` 注入 `GraphState`；Planner/Verifier/Executor 已接入 Skill 上下文；工具白名单已改为 fail-closed；已添加 `payment-oom-triage` 示例 Skill。 | 当前只有 classpath 资源加载，缺项目级覆盖、disabled state、热 reload、版本治理；生产 Skill 样例不足；未做真 tool-calling，仅保留未来演进入口。 |
| 阶段七：收敛与治理 | 骨架完成，生产资产待补 | 已新增 `KubeOnCallMetricsService`，覆盖 graph/alarm/rag/memory/skill 关键指标；`ExecutionAuditRecord.metadata` 已覆盖 Ask 和 Alarm 关键索引字段；后端运行手册已补充本地启动、测试、策略配置、RAG 重建、记忆清理、Skill 添加、metrics、最小仪表盘建议、审计查看和生产排障。 | 缺真实 Grafana/Prometheus dashboard 和 alert rules；缺 Dockerfile、Helm Chart、CI；Ask 响应仍偏文本，缺面向前端/平台消费的结构化响应。 |

## 1.2 当前仍需要重构的事项

下面是从当前代码、专项方案和架构文档中合并出的剩余工作。优先级按“先修会影响正确性的，再做效果提升，最后做交付资产”排列。

2026-07-10 已完成：旧格式告警 severity 兼容、ES 相关性顺序修复、告警 RAG filters、策略 `ragFilters` 与 8 份生产 SOP 安全绑定；SOP 幂等导入；metadata `flattened`；真实 ES mapping/写入/过滤/keyword/KNN/父聚合验收；记忆安全 cleanup、压缩、hybrid 检索、整理、软删除恢复、分布式锁、审计和告警诊断消费。

| 优先级 | 方向 | 具体事项 | 验收标准 |
| --- | --- | --- | --- |
| P1 | RAG trace | cross-encoder 主路径代码已完成；补 `RetrievalHit`、ES score/rank/channel 和阶段 latency。 | trace 含 lexical/vector/fused/rerank 分数和耗时。 |
| P1 | RAG 索引治理 | 真实 ES 主链路已通过；补 index alias、版本化 rebuild/reindex、active dataset version 和旧 chunk 清理。 | 新 embedding/dataset 可平滑切换并可回滚。 |
| P2 | 告警智能诊断增强 | 历史记忆安全消费和相对日期标准化已完成；补 LLM 证据归因和变更事件关联。 | 诊断可解释当前证据与历史处置关系，陈旧信息不会误导判断。 |
| P2 | Skill 生产治理 | Skill 支持项目目录覆盖、disabled state、reload、版本字段治理；补 payment/infra/database 等生产 Skill 样例。 | 同名项目 Skill 可覆盖内置 Skill；禁用后不会激活；reload 不重启服务。 |
| P2 | 告警策略治理 | 策略版本化、灰度、热加载、回放评估、维护窗口模型。 | 策略变更可追踪，可用历史样例回放验证误报/漏报。 |
| P3 | 可观测与交付 | 补 Grafana dashboard、Prometheus rule、Dockerfile、Helm Chart、CI；补审计查询样例。 | 新同学可按文档本地启动、运行测试、查看 dashboard、部署基础环境。 |
| P3 | API 结构化输出 | `AskResponse` 增加结构化 plan/verifier/executor/audit 字段，保留 `message` 兼容。 | 前端/平台可不解析自然语言就展示执行过程。 |

## 1.3 推荐实施顺序

### 第一批：RAG 正确性

状态：代码项已完成。

1. 已修复 ES 检索排序，移除 `createdAt` 覆盖相关性的二次排序。
2. 已给 retrieval trace 补 raw query、rewritten query、排序来源和候选文档信息。
3. 已跑 `./mvnw test`。

原因：RAG 是 Ask、告警 Runbook、记忆检索的共同底座，先保证模块边界、排序和 fallback 正确。

### 第二批：真实 embedding/kNN 与 rerank 主路径（核心代码完成）

1. 扩展 `EsKnowledgeDocumentEntity` 和 ES mapping。
2. 入库时生成 `embedding_text` 和 `embedding`，写入 embedding model/dim/version metadata。
3. `RepositoryVectorRetriever` 改为 query embedding + ES kNN。
4. cross-encoder 配置启用后作为主精排，失败回退 RRF。
完成状态：1—4 已完成；真实 ES 已使用确定性 32 维 embedding 验收 mapping、写入、metadata filter、KNN 和父文档聚合。真实 HTTP embedding/cross-encoder 联调仍待完成。

原因：这是 RAG 从“可测接口”走向“真实效果”的关键步骤。

### 第三批：记忆压缩、检索和整理

1. 新增 `TokenBudget` 和最简 `ContextCompressor`，先保护长会话不会撑爆上下文。
2. `MemoryService.search()` 复用 hybrid retrieval。
3. 新增 `MemoryConsolidationJob`，做重复合并、陈旧软删除和 metrics。
4. 补记忆质量样例和告警二次召回测试。

完成状态：1、2、3 已完成；4 待继续。50 轮长会话测试已覆盖早期关键事实和最后一轮上下文保留。

原因：记忆主链路已经能跑，下一步要降低噪声和陈旧风险。

### 第四批：Skill 与告警策略治理

1. Skill 支持项目覆盖、disabled state、reload、版本治理。
2. 补多服务生产 Skill 样例。
3. 告警策略支持版本、灰度、热加载和回放评估。
4. 将维护窗口纳入 silence / suppression 判断。

原因：这批更多是治理能力，不阻塞当前后端验收，但决定生产可维护性。

### 第五批：交付资产与结构化输出

1. Grafana dashboard / Prometheus rule。
2. Dockerfile / Helm Chart / CI。
3. `AskResponse` 结构化字段。
4. 审计查询样例和排障 Runbook。

原因：核心后端稳定后再补交付资产，避免部署模板追着内部接口反复改。

每完成一个小闭环后继续执行 `./mvnw test`，并更新 `1.1 当前进度同步` 与本节状态。

## 2. 公开接口与类型调整

### 2.1 `/api/alarms`

保持旧格式兼容，`AlarmRequest` 增加标准字段：

- `fingerprint`
- `alertName`
- `resourceType`
- `resourceName`
- `cluster`
- `namespace`
- `service`
- `metricName`
- `currentValue`
- `threshold`
- `unit`
- `duration`
- `labels`
- `annotations`
- `runbookId`
- `status`

响应保持 `List<NodeResult>`。

新增领域类型：

- `AlarmSeverity`: `P0/P1/P2/P3/INFO`
- `AlarmStatus`: `FIRING/RESOLVED/SUPPRESSED`
- `AlarmResourceType`
- `NormalizedAlarmEvent`
- `AlarmEvaluationResult`

### 2.2 `/api/ask`

`AskRequest` 增加可选 `sessionId`。

`AskResponse` 返回 `sessionId`。

无 `sessionId` 时保持现有单轮行为。

### 2.3 `/api/knowledge/query`

`KnowledgeQueryRequest` 增加：

- `topK`
- `retrieveMethod`: `KEYWORD/VECTOR/HYBRID`
- `includeTrace`

默认保持兼容旧请求。

### 2.4 工具目录

新增安全读类能力：

- `kubernetes.describeResource`
- `alertmanager.sendAlertEvent`

`alertmanager.createSilence` 保留，但只能作为审批后的显式动作，不再作为告警结果默认动作。

## 3. 阶段一：工具链与基线

目标：让项目具备可重复验证入口。

任务：

1. 添加 Maven Wrapper。
2. 明确 JDK 17 要求。
3. 默认验证命令统一为：

```bash
./mvnw test
```

4. 建立默认单测与 Docker-backed 集成测试分层。
5. 添加测试样例：
   - 旧格式告警 JSON。
   - 新格式告警 JSON。

验收：

- 默认测试不依赖 Redis/ES/MinIO。
- 集成测试可在 `docker compose up -d redis elasticsearch minio` 后运行。
- 工具链问题不会阻塞后续阶段的单测编写。

## 4. 阶段二：告警治理主线

目标：把告警链路从“上游字符串 + 固定流程”升级为“标准事件 + 策略分级 + 可审计诊断”。

任务：

1. 实现 `AlarmNormalizer`。
   - 旧字段映射到 `NormalizedAlarmEvent`。
   - 从 `metadata` 提升 `namespace/service/cluster/alertName/runbookId` 等常用字段。
2. 实现 `AlarmFingerprintService`。
   - 优先使用 `fingerprint`。
   - 其次使用 `dedupKey`。
   - 缺失时使用 `alertName + cluster + namespace + resourceType + resourceName + service + metricName + sorted stable labels` 生成 SHA-256。
3. `AlertWorkflowService` 内部改用 normalized event 和 fingerprint 去重。
4. 保留 `process(AlarmEvent)` 兼容入口。
5. 实现 `YamlAlarmPolicyRepository` 与 `AlarmPolicyEngine`。
6. 首批策略覆盖：
   - CPU
   - 内存
   - 磁盘
   - inode
   - NodeNotReady
   - PodCrashLoop
   - OOMKilled
   - DeploymentUnavailable
7. `StateCompareNode` 使用策略中的 PromQL/window。
8. `ResultPushNode` 默认只输出结构化诊断 summary，不自动 `createSilence`。

验收：

- 旧格式请求仍能执行。
- 新格式请求生成稳定 fingerprint。
- 无 `dedupKey` 时不会产生 `alarm-dedup:null`。
- CPU 69 不触发策略，70.1 触发 P1，85.1 触发 P0。
- P0/P1 默认不创建 silence。

## 5. 阶段三：工作流模板、抑制与恢复

目标：不同告警走不同诊断路径，并具备噪声治理能力。

任务：

1. 新增 `WorkflowTemplateRegistry`。
2. `AlertWorkflowFactory.buildWorkflow(workflowTemplate)` 支持：
   - `host-resource`
   - `k8s-pod`
   - `k8s-node`
   - `workload`
   - `control-plane`
3. Redis 存 active alarm：
   - `firstSeen`
   - `lastSeen`
   - `count`
   - `status`
   - `severity`
   - `fingerprint`
4. 去重 TTL 按 severity/category 配置。
5. NodeNotReady 抑制同节点 Pod 噪声。
6. P0/P1 未确认升级。
7. 支持恢复事件确认。
8. 拆分安全节点：
   - `NotificationNode`
   - `TicketNode`
   - `SilenceNode`

验收：

- 不同 `workflowTemplate` 生成不同节点序列。
- 依赖失败时降级和审计正确。
- Silence 只走审批路径。

## 6. 阶段四：RAG 重构

目标：修复当前 RAG 的数据工程问题，让后续记忆和告警 Runbook 绑定有稳定基础。

任务：

1. `KnowledgeIngestService` 保留兼容 facade。
2. 内部拆分：
   - `KnowledgeIngestionFacade`
   - `KnowledgeRetrievalFacade`
3. 标准化 metadata：
   - `doc_id`
   - `chunk_id`
   - `chunk_index`
   - `total_chunks`
   - `parent_document_id`
   - `document_type`
   - `source_type`
   - `dataset_version`
   - `chunk_enable`
   - `file_hash`
4. 替换固定 280 字符 chunker 为配置化 Markdown/recursive chunker。
5. 保存 parent 文档。
6. 检索不再按 `createdAt` 覆盖相关性。
7. 抽象：
   - `EmbeddingClient`
   - `EmbeddingService`
   - `VectorRetriever`
8. 测试使用确定性 mock embedding。
9. ES kNN 通过配置启用。
10. Rerank 接收 retrieval hit 和 score trace，失败时回退 RRF。

验收：

- parent 可回源。
- chunk metadata 完整。
- 排序不被 createdAt 覆盖。
- vector disabled/mock/failed/native-kNN 代码路径可测；真实 ES mapping、写入、过滤和 KNN 已通过集成验收。

## 7. 阶段五：记忆系统

目标：补齐会话记忆、长期记忆和告警记忆。

任务：

1. `SessionStore` 使用 Redis。
2. `AskService` 支持多轮 session。
3. 无 `sessionId` 时仍单轮。
4. 新增：
   - `MemoryEntry`
   - `MemoryType`
   - `MemoryScope`
5. 长期记忆写入 ES，`source_type=memory`。
6. 知识检索默认不被 memory 污染。
7. `MemoryInjector` 只注入：
   - `SERVICE_FACT`
   - `KNOWN_PITFALL`
8. 注入内容必须带新鲜度和“先验证当前状态”提示。
9. `MemoryExtractor` 异步从 Ask/Alarm 结果中提取记忆。
10. 提取时排除：
    - kubectl 命令
    - 实时状态
    - SOP 全文
    - 可从监控/日志重新获得的内容
11. `AlertMemoryService` 按 fingerprint/service/resource 召回历史处置。

验收：

- 同一 session 可追问。
- 长期记忆按 `source_type=memory` 隔离。
- 陈旧提示存在。
- 自动提取失败不影响主流程。

## 8. 阶段六：Skill 底座

目标：让运维经验以可加载、可版本化的 Skill 形式沉淀。

任务：

1. 实现：
   - `Skill`
   - frontmatter parser
   - `SkillRegistry`
   - `SkillMatcher`
   - `SkillActivationService`
2. 第一版 Skill 只做自动匹配和 prompt 注入。
3. 不做真 tool-calling。
4. `PlannerLlmService` 注入 skill 索引和激活正文。
5. `AgentToolCatalog` 支持 tool whitelist。
6. `VerifierThinkNode` 读取 skill `maxRisk` 联动审批。
7. 添加示例 skill：
   - `payment-oom-triage`

验收：

- 无关请求不激活。
- OOM 请求激活示例 skill。
- 工具不在激活 Skill 白名单时返回 `SKILL_TOOL_NOT_ALLOWED`，禁止回退完整工具目录。

## 9. 阶段七：收敛与治理

目标：让重构后的后端具备可运维、可追踪、可交接能力。

任务：

1. 所有新增流程写入 `ExecutionAuditService`。
2. metrics 覆盖：
   - alarm 成功率/fallback
   - RAG 命中率/fallback
   - memory 提取与注入
   - skill 激活与白名单拒绝
3. 补充后端运行文档：
   - 本地启动
   - 测试
   - 策略配置
   - RAG 重建
   - 记忆清理
   - Skill 添加
4. 清理不再使用的旧内部路径。
5. 保留公开 API 的向后兼容字段。

验收：

- 全部默认单测通过。
- 核心链路可通过 API 样例验收。
- 文档足够让后续工程师继续维护。

## 10. 测试总清单

### 工具链

- `./mvnw test` 可作为默认测试入口。

### 告警

- 旧格式请求仍执行。
- 新格式请求生成稳定 fingerprint。
- CPU 69 不触发。
- CPU 70.1 触发 P1。
- CPU 85.1 触发 P0。
- 无 `dedupKey` 不产生 `alarm-dedup:null`。

### 安全

- P0/P1 默认不创建 silence。
- `createSilence` 只有审批后节点可调用。
- `/api/alarms/silence-approvals` 可写入短期审批，过期审批不会触发 silence。

### 工作流

- 不同 `workflowTemplate` 生成不同节点序列。
- 依赖失败时降级和审计正确。
- silence 审批信息会注入工作流上下文和 Alarm 审计 metadata。

### RAG

- parent 可回源。
- chunk metadata 完整。
- 排序不被 createdAt 覆盖。
- vector disabled/mock/failed/native-kNN 代码路径可测；真实 ES 验收由 `integration-test` profile 执行。

### 记忆

- 同一 session 可追问。
- 50 轮 session 压缩后保留早期关键事实和最近结论。
- planning context 和 observations 不超过配置预算。
- 长期记忆按 `source_type=memory` 隔离。
- memory 搜索复用 hybrid retrieval，并可返回 trace。
- cleanup/consolidation/restore 有独立 Memory 审计。
- 多实例定时整理由 Redis lease lock 互斥。
- 软删除记忆可通过恢复 API 回滚。
- 陈旧提示存在。
- 自动提取失败不影响主流程。

### Skill

- 无关请求不激活。
- OOM 请求激活示例 skill。
- 工具不在激活 Skill 白名单时 fail-closed，且不生成执行载荷。

## 11. 第一批建议 PR

1. `PR-01-tooling-baseline`
2. `PR-02-alarm-normalization`
3. `PR-03-alarm-policy-engine`
4. `PR-04-safe-workflow-output`
5. `PR-05-workflow-templates`
6. `PR-06-rag-metadata-chunk-parent`
7. `PR-07-rag-retrieval-trace-vector-interface`
8. `PR-08-session-memory`
9. `PR-09-longterm-alert-memory`
10. `PR-10-skill-foundation`
11. `PR-11-docs-audit-metrics-cleanup`
