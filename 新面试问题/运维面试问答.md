
### 2. 你这个项目为什么要做成 Multi-Agent，而不是一个大模型 + 一套工具调用？

**追问：**
1. Planner、Executor、Verifier 这三个角色分别解决什么问题？如果把它们合并成一个 Agent，会先坏在哪？
2. 你说 Planner 负责跨域 Tool Plan，那“跨域”在你这个项目里具体体现在哪？请举一个真实问题例子。
3. 你的 Executor 是多个 Agent 还是一个 Agent 根据 plan 顺序执行？这里为什么这么设计？
4. Verifier 放在链路末端拦高危动作，这种方式会不会太晚？为什么不是“规划时就拦”、“执行前每步都拦”？

## 二、告警 Workflow / DAG 深挖

### 3. 你说“采用 DAG 结构实现告警触发后的固化排障流水线”，那这个 DAG 的节点一般长什么样？节点之间的依赖关系怎么定义？

**追问：**
1. 你这个 DAG 是静态预定义的，还是根据告警内容动态生成的？为什么？
2. 一个典型链路“日志取数 → 状态比对 → RAG 检索 → 结果推送”，为什么这个顺序合理？
3. 如果“日志取数”失败了，后面的节点还跑不跑？这是节点级策略还是全局策略？
4. 节点之间传递的上下文长什么样？是统一上下文对象还是每个节点自己拼 map？
5. 如果以后新增一个“自动执行修复脚本”节点，你怎么避免把整个 DAG 改得很脆？



---

### 4. 你提到“状态机 + 幂等”防止告警抖动导致重复执行，具体是怎么做的？

**追问：**
1. 你这里的幂等键到底是 `alarmId`、`dedupKey`，还是两者组合？你是怎么定义业务唯一性的？
2. 为什么选 Redis `SETNX` 做去重，而不是落 MySQL 唯一键或者本地内存去重？
3. 这个幂等窗口 TTL 怎么定？设短了会漏拦，设长了会误杀，怎么平衡？
4. 如果同一个告警先后两次来，但第二次其实是问题加重、需要重新处理，你的去重会不会把真正需要处理的告警也吃掉？
5. 你怎么区分“告警抖动重复”和“相同源头但新一轮真实故障”？

**延伸追问：**
6. 如果 Redis 短暂故障或主从切换，`SETNX` 失败或超时，你的去重链路怎么兜底？
7. 如果出现重复消费，除了消息层去重，业务层还有没有第二道保险？



---

### 5. 你说节点级有“超时干预与失败降级策略”，具体怎么设计？

**追问：**
1. 哪些节点适合降级继续，哪些节点必须失败终止？你划分标准是什么？
2. 例如“设备信息查询失败”，后面 RAG 还要不要继续？如果继续，回答质量怎么保证？
3. 你怎么定义一个节点的超时阈值？不同节点阈值一样吗？
4. 如果一个长周期脚本已经在底层执行了，但上层节点超时了，你怎么避免“上层判失败、底层其实还在跑”导致状态错乱？
5. 降级后最终响应里，你会不会显式告诉用户“哪些信息缺失、哪些结论可靠度下降”？


---

## 三、Agent 执行链路深挖

### 6. 你说单个智能体内部采用 ReAct 循环“取证→校验→调整”，请你讲一个真实例子，说明这三步分别做了什么。

**追问：**
1. “取证”阶段通常会调哪些工具？是日志、指标、配置，还是知识库？
2. “校验”是让模型自己判断前一步结果是否可信，还是用规则校验？
3. “调整”是指重新选工具、改查询参数，还是改推理路径？
4. ReAct 最多循环几轮？你为什么设这个上限？
5. 如果连续几轮都拿不到关键信息，你是触发 replan、直接失败，还是给用户不确定答复？



---

### 7. 你怎么防止 Agent 在循环里“越想越偏”、最后跑飞？

**追问：**
1. 你判断“跑偏”的信号是什么？是循环次数超限、重复调用无效工具、还是结论置信度太低？
2. 如果 Agent 一直反复调用同类工具，比如连续查日志、查日志、还是查日志，你怎么识别这种低价值循环？
3. 你会不会记录每一轮 thought / action / observation？这些记录是为了调试，还是为了审计？
4. 什么时候触发 replan？replan 之后是从头来，还是继承之前证据？
5. 如果 replan 后还是不收敛，你最终给用户什么回复才算负责任？


---

### 8. 你这里 Planner 负责生成 Tool Plan，那 Planner 的输出结构是什么？它只是自然语言步骤，还是结构化计划？

**追问：**
3. 这个 Tool Plan 是一次性完整生成，还是边执行边补计划？
4. 如果 Planner 规划出一个不存在的工具，或者参数不合法，谁来兜底？
5. 你是怎么降低 Planner 幻觉出假工具、假参数的概率的？


---

## 四、HITL / 审批 / 安全边界

### 9. 你说高危动作会触发 Verifier 拦截并进入 HITL，Verifier 到底怎么判断“高危”？

**追问：**
1. 是规则引擎、关键词匹配、风险打分，还是再让一个模型判断？
2. 如果模型建议“删除临时文件”这种动作，它到底算低危还是高危？你的边界怎么画？
3. 你怎么防止模型把危险动作伪装成无害描述，比如“清理异常状态”“恢复默认配置”？
4. 如果一条动作同时包含安全步骤和危险步骤，是整体拦截还是拆开审批？
5. 你这个 Verifier 是只看最终动作，还是连中间 plan 也会看？为什么？



---

### 10. 你说图状态会强制挂起（State Pause），并把上下文序列化落盘。这里具体保存了什么？

1. 你保存的是完整执行上下文，还是最小恢复上下文？怎么权衡？
2. 除了用户问题和 plan，你还会保存哪些中间证据？工具结果、审批原因、节点状态会保存吗？
3. 这些上下文放 Redis、MinIO、数据库还是本地文件？为什么？
4. 如果审批迟迟不来，这个挂起状态什么时候过期？过期后怎么处理？
5. 如果系统重启后恢复审批流，你怎么保证能从正确位置恢复，而不是重复执行危险动作？



### 11. 你怎么防止 HITL 恢复后“危险动作被重复执行”或者“审批通过后上下文已经过期”？

**追问：**
1. 审批通过时，你恢复的是“从暂停点继续”，还是“重新整体跑一遍再跳过前面步骤”？
2. 如果暂停期间环境已经变化，比如服务已经恢复、配置已经改过，那原计划还有效吗？
3. 你会不会在恢复执行前再做一次环境确认？如果会，做到什么粒度？
4. 如果审批结果是批准，但恢复前关键证据已经过期或者缺失，你是继续执行、重新取证，还是拒绝恢复？
5. 这个机制有没有可能造成用户体验很差，比如审批通过了结果系统说“上下文过期，无法继续”？你怎么解释这个取舍？



---

## 五、RAG 设计与检索效果深挖

### 12. 你说 RAG 流程是“问题改写 → 混合检索 → 重排序”，那为什么不是用户原问题直接检索？

**追问：**
1. 什么情况下必须做 Query Rewrite？什么情况下你反而不敢改写？
2. 如果用户问题里包含告警码、IP、Pod 名、容器名这些精确 token，改写会不会把关键字改丢？
3. 你的 Rewrite 是偏同义改写、补全上下文，还是转标准化检索语句？
4. 如果改写前后的语义偏了，最终谁兜底？
5. 你有没有做过“原问题检索 vs 改写后检索”的效果对比？哪类问题收益最大？



---

### 13. 你说“解释类走 RAG，查询设备指标走 planner”，这个路由是怎么做的？

**追问：**
1. 路由规则是规则分类、模型分类，还是混合判定？
2. 如果用户问“为什么这台机器 CPU 高，顺便给我看看最近 10 分钟负载”，这既像解释类又像工具查询类，你怎么路由？
3. 路由错误的后果是什么？最坏是答非所问还是执行错工具？
4. 你有没有给路由结果做兜底，比如双路尝试、置信度阈值、失败回退？
5. 如果面试官问：这个路由是不是过早优化？你怎么证明它确实必要？



---

### 14. 你为什么采用 Parent-Child 分块？父块和子块分别多大？怎么定的？

**追问：**
1. 你为什么不直接固定长度分块？
2. Parent-Child 的收益是什么？是召回率提升、上下文完整性更好，还是减少 chunk 噪声？
3. 子块命中后为什么还需要回父块？直接把子块喂给模型不行吗？
4. 如果父块太大，会不会把无关内容也带进来，反而增加 hallucination？
5. 你的 overlap 怎么定？overlap 太大和太小分别有什么副作用？



---

### 15. 你说 Metadata 过滤用了“节点角色 / K8s 版本”，这个过滤在检索里起了什么作用？

**追问：**
1. 这些 metadata 是人工整理的还是自动抽取的？
2. 如果 metadata 打错了，会不会造成“明明有文档却检不到”？
3. 过滤条件是在召回前做、召回中做、还是召回后做？为什么？
4. 你怎么处理用户没显式给出节点角色，但上下文里其实已经知道的情况？
5. metadata 过滤会不会造成召回过窄，错失一些本来有帮助的通用 SOP？


---

### 16. 你说 ES 里做了“BM25 + 向量”双路召回，为什么不直接上一个纯向量库？

**追问：**
1. 你说 BM25 用来兜底特殊告警码、IP、容器名强命中，请举例说明如果没有 BM25 会发生什么问题。
2. 你双路召回的结果怎么融合？是简单拼接，还是 RRF？为什么？
3. 向量检索在你的实验室场景里，真正带来的收益是什么？
4. 既然是实验室内部系统，文档量不大，为什么还值得做混合检索？
5. 如果面试官追问：你这个系统里向量召回是不是收益其实不大、主要还是 BM25 在干活？你怎么回答？



---

### 17. 你提到 RRF 和 Cross-Encoder 做二次重排，这两个东西分别解决什么问题？

**追问：**
1. RRF 为什么适合放在双路召回后？
2. Cross-Encoder 的收益和成本分别是什么？
3. 在你这个系统里，Cross-Encoder 是全量重排还是只排 topN？为什么？
4. 如果在线延迟突然上升，你先砍掉哪一层：向量检索、RRF、Cross-Encoder，还是 Query Rewrite？为什么？
5. 你有没有做过“不开 Cross-Encoder”和“开 Cross-Encoder”的效果对比？



---

## 六、Tool Calling / MCP / 外部系统接入深挖

### 18. 你项目里工具调用能力是怎么设计的？工具注册、工具描述、参数校验、权限控制分别怎么做？

**追问：**
1. Tool description 你怎么写，才能让模型少误调用？
2. 如果两个工具功能很像，比如“查设备状态”和“查容器状态”，模型怎么选？
3. 参数校验是在模型输出后做 schema 校验，还是工具内部兜底？
4. 权限校验是在 Agent 层做、工具网关层做，还是工具实现层做双重校验？
5. 如果一个工具调用外部接口失败，你返回给模型的错误信息要多详细？太详细会不会诱导模型乱补参数？


---

### 19. 你提到 MCP，那你项目里 MCP 起到的核心作用是什么？它和直接 function calling 有什么本质区别？

**追问：**
1. 你为什么需要 MCP，而不是自己写一层 HTTP 工具注册中心？
2. MCP 更适合解决“工具接入标准化”还是“工具执行安全性”？
3. 如果去掉 MCP，只保留 Tool Executor，这个项目还能正常工作吗？
4. 你觉得 MCP 在你这个项目里是核心能力，还是偏工程便利层？
5. 站在面试官视角，我怎么判断你不是只会说 MCP 这个词？你觉得你最该用哪个实操细节证明自己真的做过？



---

### 20. 假设某个工具是“执行服务器修复脚本”，你怎么控制超时、重试、幂等和熔断？

**追问：**
1. 为什么这类工具不能简单失败就自动重试？
2. 如果脚本已经执行了一半，但客户端超时了，你怎么判断能不能重试？
3. 幂等键怎么设计？按请求 ID、故障 ID、还是动作签名？
4. 熔断触发后，Agent 是继续换方案，还是直接升级人工？
5. 如果模型反复想调用这个失败工具，你怎么阻止它“执念式重试”？


---

## 七、后端能力与工程治理深挖

### 21. 你这个系统如果在线上出现 CPU 飙升、接口超时、工具调用堆积，你怎么排查？

**追问：**
1. 先看应用线程池、Redis、ES、LLM 接口、还是工具网关？你的排查顺序是什么？
2. 怎么快速判断是“大模型慢”还是“工具慢”还是“RAG 慢”？
3. 如果 Planner 线程很多都卡住了，你怀疑什么？
4. 如果告警流没堆积，但问答流 RT 飙高，你优先看哪几段链路？
5. 你会打哪些核心监控指标来证明自己不是靠猜？




### 22. 你怎么做可观测性？尤其是 Agent 系统这种链路长、步骤多、失败形态复杂的系统。

**追问：**
1. 你会记录哪些核心指标：请求数、成功率、平均轮数、工具成功率、审批命中率、RAG 命中率？
2. 你怎么区分“系统成功回答了”和“系统回答了但其实没价值”？
3. 你有没有记录 Agent 每轮执行轨迹？是全文存，还是摘要存？
4. 如果用户抱怨“系统总在绕圈子”，你靠什么日志或指标定位？
5. 你怎么证明“自动化处理 100 余次”不是低价值请求堆出来的数字？



---

### 23. Redis 在你项目里扮演了哪些角色？如果 Redis 出故障，哪些功能先受影响？

**追问：**
1. 你这里只把 Redis 当缓存，还是还承担状态机、幂等控制、审批状态存储？
2. 哪些 Redis 数据可以丢，哪些绝对不能随便丢？
3. 如果 Redis 故障，告警去重会怎样？审批恢复会怎样？Agent 状态会怎样？
4. 你有没有做 Redis key TTL 设计？为什么有些状态必须有 TTL，有些不能轻易过期？
5. 如果面试官追问：你这个项目是不是过度依赖 Redis 了？你怎么回应？



---

### 24. 你项目里用了 MinIO 和 Elasticsearch，它们各自承载什么数据？为什么这样划分？

**追问：**
1. MinIO 里放原始文档还是切分后的 chunk，还是两者都放？
2. Elasticsearch 里是只存索引，还是也保留部分内容快照？
3. 如果 MinIO 里的原始文档更新了，ES 索引怎么同步？
4. 如果 ES 索引和 MinIO 原文不一致，最终以谁为准？怎么补偿？
5. 这个链路里有没有可能出现“文档已删但索引还在”的脏数据？你怎么治理？


---

## 八、AI 应用开发能力评估

### 25. 你觉得一个真正可落地的 Agent 系统，和 demo 级 Agent 最大的差别是什么？

**追问：**
1. 你这个项目里，哪些地方最能体现“不是 demo”？
2. 如果只做一个能调用工具的 chatbot，离生产可用还差哪些东西？
3. 你觉得 Agent 工程化里最容易被忽略的坑是什么？
4. 你做完这个项目后，对“大模型不可靠”这件事最大的工程体会是什么？
5. 如果让你再做一版，你最先加强的是规划能力、检索能力、安全能力，还是可观测性？为什么？


---

### 26. 你怎么理解 hallucination？你项目里真正有效的幻觉治理手段有哪些？

**追问：**
1. RAG、Tool Calling、Verifier、HITL，这几个手段分别在治理哪一类幻觉？
2. 有了 RAG 为什么还会幻觉？
3. 如果模型拿到检索结果后还是胡说，你靠什么约束它？
4. 如果工具返回空结果，模型却自己脑补了一个结论，你怎么发现？
5. 你有没有设计“拒答”或“降级答复”机制，而不是强行输出一个看起来完整的答案？



---

### 27. 你在这个项目里如何评估 AI 能力真的带来了收益，而不是引入了复杂度？

**追问：**
1. 你会看哪些指标：平均排障时间缩短、人工接管率下降、答复可采纳率提升，还是别的？
2. 对实验室这种低并发内部场景，你怎么证明投入做 Agent 是值得的？
3. 如果最后发现 80% 请求都可以用固定流程搞定，那你这个 AI 部分是不是做重了？
4. 你会如何回答“这个项目的 AI 含量到底有多少，核心价值到底在哪”？
5. 如果让你站在面试官视角，你最怀疑候选人吹牛的地方会是哪块？你怎么自证？



---

## 九、连续施压追问（专门测真实性和深度）

### 28. 你说你的防误操作是通过 Verifier + HITL 做的。那我继续追问：如果高危动作在 plan 里被拆成多个低危动作，每个动作单看都不危险，但合起来很危险，你怎么识别？

**连续追问：**
1. 你是按单步动作审，还是按整条执行链审？
2. 如果模型把“删目录、重建配置、重启服务”拆成三个步骤，你怎么知道这其实是一组高风险组合拳？
3. 你的风险识别是基于关键词，还是基于资源对象 + 动作语义？
4. 如果候选人回答“目前主要靠规则”，那我会继续问：规则怎么避免被绕？
5. 如果回答“再加一个模型审核”，我会追问：那第二个模型幻觉怎么办？



---

### 29. 你说用状态机 + 幂等来抗告警抖动。那我继续追问：如果同一条告警先触发一次自动处理，后面又来了升级告警，但 dedupKey 设计得太粗把它吞了，这不是会漏处理吗？

**连续追问：**
1. 你的 dedupKey 到底包含哪些维度？告警源、级别、时间窗口、资源对象、症状会不会都进来？
2. key 设计细了会导致去重失效，设计粗了会导致漏处理，你怎么拿捏？
3. 有没有做“首次命中去重但允许升级告警穿透”的机制？
4. 如果现在没有做，你会怎么补？
5. 如果用户质疑这是个严重缺陷，你会怎么诚实但专业地回应？

**考察点：**
- 幂等设计是否只是表面功夫
- 是否能承认边界并提出改进方案

---

### 30. 你说 RAG 做了 Parent-Child、Metadata、BM25+向量、RRF、Cross-Encoder。那我继续追问：这么多层叠加下来，收益真的是每层都明显吗？还是“看起来很全”？

**连续追问：**
1. 如果我让你删到只剩两层，你保留哪两层？为什么？
2. 你有做过 ablation study（消融对比）吗？
3. 如果没有，你怎么证明不是“把流行词堆满”？
4. 实验室文档量不大时，Cross-Encoder 的收益到底值不值这个延迟？
5. 如果线上压测发现性能吃不消，你最先砍谁？为什么？

### 31. 你说 Agent 用 ReAct 循环“取证→校验→调整”。那我继续追问：如果用户已经拿着旧上下文在操作，你的系统会不会出现‘基于过期证据做新决策’的问题？

**连续追问：**
1. 你怎么判断某条 observation 过期了？
2. 如果一轮取证拿到的日志是 5 分钟前的，下一轮还能继续基于它做决策吗？
3. 你的工具结果有没有时间戳 / 版本号 / 快照 ID？
4. 在高危动作前，你会不会强制重新取证，而不是依赖旧证据？
5. 如果候选人回答“目前实验室场景先没做这么细”，我会继续问：那你至少意识到这个问题了吗？打算怎么补？

---

### 32. 你说每次客户端发写请求时，都带上当前已知的最新版本号，服务端收到后先比对，如果发现不是期望版本就直接拒绝，不处理也不保存，相当于把“过期”的写操作拦了。你这样不会导致旧版本用户的更新操作失效，用户体验很差吗？

**连续追问：**
1. 你为什么选择“强校验直接拒绝”，而不是自动合并或者 last write wins？
2. 你的版本号是资源级、字段级，还是整个会话级？粒度不同冲突率完全不同，你怎么选？
3. 如果旧版本用户只是改了一个和别人不冲突的小字段，也直接拒绝，是不是太粗暴？
4. 你有没有给前端返回“冲突详情”，让它提示用户刷新后重试，而不是只报 409？
5. 如果这是实验室内部系统，人不多，你会不会反而更倾向简单保守的实现？为什么？


