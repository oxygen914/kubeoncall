
## 一、项目定位与真实性深挖

### 1. 你先不要讲亮点，直接用 3 分钟讲清楚智图这个项目到底解决了什么问题？为什么实验室会需要这样一个系统？

**追问：**
1. 你这里说的“高分遥感图、架构拓扑、监控快照”分别是什么场景下产生的？
2. 为什么这些东西不能直接丢网盘或共享文件夹里管理？真正的痛点是“存储不下”“找不到”“权限乱”还是“多人不能协同”？
3. 你说这是多人协同图库，那它和普通 OSS 图床、企业网盘、在线文档系统的本质区别是什么？
4. 这个系统目前实际有多少人使用？高峰期会出现多少并发？如果用户量就十几个人，为什么还值得做这么多能力？
5. 如果面试官追问：这个项目是不是把很多流行技术名词都堆上去了？你怎么证明它不是“包装式项目”？


---

### 2. 你这个系统里，哪一部分最难？如果只能保留一个核心能力，你会保留哪个？

**追问：**
1. 是大图上传、协同防脏写、多租户权限，还是 Agent 图片处理最难？为什么？
2. 如果只保留大图上传，这个系统还成立吗？
3. 如果只保留 Agent 图片处理，不做协同和权限隔离，这个系统还有多少业务价值？
4. 你是怎么判断一个功能是“项目亮点”还是“写在简历上好看但真实价值没那么高”的？
5. 如果面试官质疑“AI 引擎只是附加项，真正难的是后端基础设施”，你同不同意？为什么？

---

## 二、大文件上传 / MinIO / Redis 状态机深挖

### 3. 你说做了 GB 级图片的分块上传，那你的 chunk size 和并发分片数是怎么定的？

**追问：**
1. 为什么不是 1MB、5MB、64MB，而是你最后选的那个区间？
2. 单图大概 1GB 左右的话，大概会切成多少片？这个数量会不会给前端管理、Redis 状态记录带来压力？
3. 并发分片数为什么不能拉太高？瓶颈在浏览器、网关、MinIO，还是后端签发流程？
4. 如果实验室网络波动大，分片特别容易失败，你会优先调大 chunk 还是调小 chunk？为什么？
5. 如果面试官继续追问：你这些参数是压测出来的，还是工程经验值？你怎么回答才既真实又专业？



---

### 4. 你说秒传是基于 MD5 指纹做的，那 MD5 的碰撞风险和性能收益你是怎么权衡的？

**追问：**
1. 为什么选 MD5，不选 SHA-256？
2. 你是把 MD5 当安全校验，还是当工程上的文件去重标识？这两个语义能不能混？
3. 如果两个不同文件极端情况下碰撞了，会发生什么后果？
4. 你有没有叠加文件大小、后缀、租户信息这些辅助条件，降低误判风险？
5. 如果面试官问：既然是实验室内部系统，文件量不算海量，秒传真的有必要吗？你怎么回答？



---

### 5. 预签名 URL 直传 MinIO 时，如何限制对象路径、防越权上传、防跨租户污染？

**追问：**
1. 对象路径是前端传给后端，还是后端直接生成？为什么？
2. 如果前端自己拼对象路径，会带来什么风险？
3. 你的路径里会不会天然带 `tenantId/userId/uploadId` 这类信息？为什么？
4. 预签名 URL 的有效期怎么定？太长和太短分别有什么问题？
5. 如果一个用户拿到了另一个用户的 URL，他能不能往别的对象路径上传？你是怎么限制的？

**延伸追问：**
6. 如果 URL 泄露了，你有没有办法让它快速失效，还是只能等自然过期？
7. 如果面试官追问：你这里真正的租户隔离是在 MinIO 层做的，还是在业务系统签发 URL 时做的？你怎么讲清楚？



---

### 6. 你说 Redis 状态机持久化切片索引，实现跨会话断点续传。这个状态机具体怎么设计？

**追问：**
1. 一个 uploadId 下面你至少会存哪些字段？总片数、已完成片集合、文件 MD5、用户信息、租户信息会不会都存？
2. 为什么这个状态要放 Redis，而不是前端自己记，或者 MySQL 落库？
3. 客户端断线重连后，后端怎么告诉前端哪些分片还缺？
4. 如果客户端重复上传同一片，怎么保证状态不被写乱？
5. 这些状态 key 的 TTL 怎么定？上传到一半人走了，状态永久不删会不会越来越脏？


---

### 7. 分块上传异常重试时，幂等性怎么保证？

**追问：**
1. 你怎么定义“一个分片上传成功”这件事？以 MinIO 落对象为准，还是 Redis 状态写成功为准？
2. 如果 MinIO 已经写成功了，但 Redis 状态还没更新，客户端又重试，这时候会发生什么？
3. 你是按 `chunkIndex` 去重，还是按分片内容哈希去重？
4. 如果分片乱序到达，或者某一片迟迟没传上来，最终合并逻辑怎么保证正确？
5. 合并前你会不会做“所有分片齐全”的校验，而不是相信前端说“我传完了”？



---

## 三、Kafka / 最终一致性 / DLQ 深挖

### 8. 你说文件上传成功后会投递 Kafka 事件，异步触发 AI 脚本处理和 ES 建索引。为什么这里一定要异步，不能同步做完？

**追问：**
1. 这里的慢动作主要慢在哪？是图像预处理、特征提取、VLM 解译，还是 ES 建索引？
2. 如果这些动作都同步做，会对用户上传接口造成什么影响？
3. 你的上传成功语义到底是什么？是“文件存下来了就算成功”，还是“后处理也完成才算成功”？
4. 如果后处理失败了，用户在前台看到的状态是什么？
5. 你有没有做“上传成功但处理中”的中间态？为什么这个状态对用户很重要？



---

### 9. 你说利用事务消息机制保障 MySQL 元数据和 ES 最终一致。那这个一致性链路你具体怎么设计的？

**追问：**
1. 为什么 MySQL 是主数据，ES 只是检索副本？
2. 如果 MySQL 写成功了，但消息没发出去，会出现什么后果？
3. 你这里的事务消息是怎么和业务事务协同的？核心要避免的到底是哪一种双写不一致？
4. 如果消息成功发出，但消费者处理失败了，最终靠什么机制把 ES 追平？
5. 如果 ES 里已经有一份旧索引，新的消费重放来了，怎么避免索引越写越脏？



---

### 10. Kafka 消费失败时，你的重试策略和 DLQ 触发条件是什么？

**追问：**
1. 你怎么区分临时性失败和永久性失败？
2. 像 ES 暂时不可用、脚本服务超时，这种适合重试几次？
3. 像对象不存在、文件损坏、参数错误，这种为什么不应该一直重试？
4. 进入 DLQ 之后，后面是人工排查、自动补偿，还是支持选择性重放？
5. 如果面试官继续追问：DLQ 里的消息你敢不敢“一键全量重放”？为什么？



---

### 11. DLQ 里的消息如果要重放，怎么避免“重放再次污染索引”或者“过期消息把新状态覆盖掉”？

**追问：**
1. 你会不会在重放前先看 MySQL 当前主数据状态？为什么？
2. 消费逻辑是不是必须幂等？幂等边界怎么定义？
3. 同一个文件的索引更新你是新增一条，还是按唯一标识覆盖写？
4. 如果这个文件在进入 DLQ 之后已经被删除了，还能不能重放？
5. 如果面试官追问：你这里是否需要版本号 / 更新时间来防止旧消息覆盖新索引？你怎么回答？



---

## 四、多租户 / JWT / ThreadLocal 深挖

### 12. 你说结合 JWT 与 Redis 做鉴权中枢，那 JWT 和 Redis 在这个系统里分别承担什么职责？

**追问：**
1. JWT 里放的是身份、权限，还是两者都会放？
2. 如果只靠 JWT，不结合 Redis，会有什么问题？
3. 为什么权限变更不能完全依赖 JWT 自然过期？
4. Redis 里你会存会话状态、权限版本，还是黑名单？
5. 如果 Redis 挂了，你的鉴权是直接拒绝、降级只验 JWT，还是别的策略？为什么？


---

### 13. 你说设计了公共域、共享域、私有域的数据边界，那这个 RBAC 模型具体怎么落地？

**追问：**
1. 用户、租户、资源、角色，这几个关系你是怎么组织的？
2. 同一张图片如果在共享域里，多个人能看到，那编辑权限和查看权限怎么区分？
3. 你是只在接口层做权限校验，还是对象路径、查询条件、索引检索也都要带租户边界？
4. 如果一个用户同时属于多个课题组，租户上下文怎么选？
5. 如果面试官追问：多租户隔离做得不好，最坏后果是什么？你怎么回答才能体现风险意识？


---

### 14. 你说用自定义拦截器 + ThreadLocal 透传用户身份和租户 ID。那 ThreadLocal 最大的风险是什么？你怎么防线程池污染？

**追问：**
1. 为什么 ThreadLocal 在线程池里容易串租户？
2. 你在请求结束时是怎么清理的？为什么必须放 finally？
3. 如果涉及异步线程池，它会自动继承主线程上下文吗？
4. 你是显式传递 tenantId，还是做线程池包装？为什么？
5. 如果面试官继续追问：一旦串租户，最坏可能出什么事故？你怎么回答？



---

### 15. 权限变更怎么做到秒级生效？又怎么避免缓存抖动？

**追问：**
1. 如果一个用户权限刚被收回，但旧 token 还有效，你怎么防止他继续操作？
2. 你这里做的是全局缓存失效，还是按用户 / 会话粒度更新？为什么？
3. 如果每次请求都查 Redis，会不会有性能压力？
4. 如果面试官追问：你这里是不是把 Redis 当“在线权限源”了？这样会不会过度依赖 Redis？
5. 如果权限变更和 JWT 续签同时发生，怎么避免短时越权？



---

## 五、WebSocket 协同 / Edit Lock / 防脏写深挖

### 16. 你说多人在线标注要防脏写，那你的“防脏写”到底是怎么实现的？

**追问：**
1. 你这里防的是哪一类脏写？是多人同时改同一张图，还是同一张图的同一区域、同一图层？
2. 你为什么选择 Edit Lock，而不是乐观锁版本号方案，或者 OT / CRDT？
3. 你的锁粒度是整张图、局部区域，还是某个编辑会话？粒度不同体验和复杂度完全不一样，你怎么选？
4. 如果两个人几乎同时点进编辑区抢锁，谁赢由什么决定？
5. 如果一个人拿着锁一直不操作，别人是不是一直得等？你怎么处理这种体验问题？



---

### 17. 你说基于 Redis 做 Edit Lock + 心跳续期，那锁过期时间和续期机制怎么设计？

**追问：**
1. 为什么锁一定要有 TTL？
2. TTL 设太短和设太长分别有什么问题？
3. 心跳是前端多久发一次？为什么不是每次操作时才续？
4. 服务端续期时，怎么确认“只有当前锁持有者自己”才能续？
5. 如果心跳偶发丢失，你会单次失败就释放锁吗？为什么？



---

### 18. 如果心跳丢失了，你怎么避免“误释放”或者“锁悬挂”？

**追问：**
1. 你是按单次心跳失败判死，还是按 TTL 自然过期判死？为什么？
2. 如果实验室网络抖一下，用户其实还在线，这时候怎么减少误判？
3. 如果锁已经过期，另一个人拿到了锁，前一个人的页面又恢复了，它再发心跳怎么办？
4. 你有没有给前端提示当前锁快过期或者锁已失效，而不是等用户改完才报错？
5. 如果面试官追问：这种方案是不是更偏“保守串行”，不追求很丝滑的协同体验？你怎么回应？



---

### 19. 你这里多人并发标注冲突时，是采用乐观合并还是强锁串行？为什么？

**追问：**
1. 为什么你没有上 OT / CRDT？是不需要，还是成本太高？
2. 对图片标注场景来说，为什么强锁串行可能更合理？
3. 如果两个人改的是不同区域，理论上不冲突，你为什么还可能选择保守方案？
4. 如果以后用户规模变大、协同更频繁，你觉得这个方案最先会暴露什么瓶颈？
5. 如果面试官质疑“这个方案不够高级”，你怎么解释这是工程取舍而不是能力不足？


---

### 20. 你的防脏写如果进一步升级成“版本号校验”，那我继续追问：你说每次客户端发写请求时，都带上当前已知的最新版本号，服务端收到后先比对，如果发现不是期望版本就直接拒绝，不处理也不保存，相当于把“过期”的写操作给拦了。你这样不会导致旧版本用户的更新操作失效，导致操作失败体验很差吗？

**连续追问：**
1. 你为什么选择“直接拒绝”，而不是自动合并或者 last write wins？
2. 你的版本号粒度是整张图、一个标注对象，还是某个区域？粒度越粗，冲突率是不是越高？
3. 如果用户只是改了一个不冲突的小字段，也直接因为版本过期被拒绝，会不会太粗暴？
4. 你有没有给前端返回冲突详情，让它提示“已有他人更新，请刷新后重试”，而不是单纯给个 409？
5. 如果这是实验室内部系统，人不多、数据比体验更重要，你会不会更偏向保守拒绝策略？为什么？

---

## 六、WebSocket 鉴权 / 重连风暴 / 实时信令深挖

### 21. 你说 WebSocket 握手阶段用了自定义拦截器做前置鉴权。为什么这里必须前置鉴权，而不是先连上再校验？

**追问：**
1. 如果握手不做严格鉴权，最坏会发生什么问题？
2. 握手时你校验的是 token、租户、会话，还是编辑权限都会一起校验？
3. 如果 token 过期了，握手失败时你怎么把失败原因传给前端？
4. 已建立的连接，如果后续权限被收回了，怎么处理？
5. 如果面试官追问：WebSocket 鉴权是不是比普通 HTTP 更难做，你觉得难点主要在哪？



---

### 22. 如果实验室网络抖动导致多人同时掉线重连，怎么防止“重连风暴”？

**追问：**
1. 为什么不能让前端无脑立刻重连？
2. 你的客户端退避策略是固定间隔、指数退避，还是带随机抖动？为什么？
3. 如果一个用户连续很多次鉴权失败，是不是还让它一直重连？
4. 服务端有没有必要对握手失败次数做限流或短期黑名单？
5. 如果面试官继续追问：用户量就十几个人，真有必要考虑这个吗？你怎么回答？



---

## 七、Agent 图片处理引擎深挖

### 23. 你说做了分层 Agent 协作中枢（Base / ReAct / ToolCall），这三层分别承担什么职责？

**追问：**
1. Base 层解决的是通用能力封装，还是执行生命周期管理？
2. ReAct 层在图片处理任务里具体怎么体现“Reason + Act”？
3. ToolCall 层只是把工具挂给模型，还是还负责参数校验、异常包装、权限边界？
4. 为什么图片处理场景要用 Agent，而不是一个固定 DAG 把脚本串起来？
5. 如果面试官质疑：你这部分是不是“脚本工作流硬包装成 Agent”？你怎么回应？



---

### 24. 你说把底层图像解析脚本和 eo-learn 的 EOTask 封装成大模型可调用工具。那工具注册与调用边界怎么设计？

**追问：**
1. 为什么要把底层脚本封装成原子工具，而不是直接让模型拼 shell 命令？
2. 这些工具的 description 怎么写，才能让模型少误用？
3. 工具参数怎么约束？是 schema 校验，还是工具内部兜底？
4. 如果某个工具执行很耗时甚至失败，你返回给模型的错误信息会有多详细？
5. 如果工具本身有副作用，比如生成中间文件、改元数据，你怎么保证它被安全调用？


---

### 25. 你说 Agent 会自主编排“影像预处理 → 算子特征提取 → VLM 视觉解译 → 研报生成”。这里为什么不直接写死成固定流程？

**追问：**
1. 哪些步骤是稳定固定的，哪些步骤真的需要 Agent 动态决策？
2. 如果用户上传的是普通监控快照，不是遥感图，这条链是不是还一样？
3. 你这里 Agent 的价值更偏“动态选择工具”，还是“动态生成分析过程”？
4. 如果模型在规划时选错工具，比如本来该跑 NDVI 却跑了云检测，你怎么兜底？
5. 如果面试官继续追问：你这个场景是不是用规则路由 + 固定流水线就够了，为什么还要 Agent？你怎么回答？



---

### 26. 你怎么防止 Agent 在图片分析任务里“乱调用工具”或者“分析看起来很专业其实不可靠”？

**追问：**
1. 你有没有做工具白名单、参数校验、调用次数限制？
2. 如果一个工具返回空结果或者异常结果，模型会不会自己脑补？怎么防？
3. 你有没有做多步结果校验，比如特征提取结果异常时不允许直接进研报生成？
4. 如果面试官追问：VLM 输出的解释本身也可能不稳定，你最后给用户的是“确定结论”还是“带不确定性说明的分析结果”？
5. 在实验室内部场景里，你觉得 AI 最需要被约束的点是什么：乱调工具、乱下结论，还是乱生成看起来很像真的报告？



---

## 八、后端工程能力与可观测性深挖

### 27. 如果线上出现“上传成功率下降、断点续传异常增多、Kafka 积压上升、WebSocket 掉线增多”，你怎么排查？

**追问：**
1. 你先看应用日志、Redis、MinIO、Kafka、WebSocket 网关，还是数据库？为什么？
2. 怎么判断是上传链路有问题，还是后处理链路有问题？
3. 如果 Redis 状态机出问题，会反映在什么现象上？
4. 如果 Kafka 积压上升但 MySQL 正常，优先怀疑消费者、AI 脚本，还是 ES？
5. 你会打哪些关键指标，来证明你不是靠猜在排查？


---

### 28. 你怎么设计这个系统的可观测性指标？尤其是协同标注和异步处理这两条链路。

**追问：**
1. 上传链路你会关注哪些指标？上传成功率、平均上传时长、断点续传命中率、秒传命中率会不会都看？
2. Kafka 链路你会看生产量、消费延迟、重试次数、DLQ 数量吗？为什么？
3. 协同链路你会看哪些指标？WebSocket 当前连接数、握手失败数、锁续期失败数、锁冲突次数？
4. 这些指标里，哪些最能直接反映用户体验变差？
5. 如果告警触发太频繁，你怎么做分级，而不是让自己被告警风暴淹没？


---

### 29. 你觉得这个项目里最容易被忽略、但一旦出问题后果会很严重的点是什么？

**追问：**
1. 是预签名 URL 越权、ThreadLocal 串租户、锁误释放、消息重放污染索引，还是 AI 乱生成报告？
2. 你为什么觉得这个点最危险？
3. 这个问题如果真的发生，用户第一时间会感知到吗，还是会隐蔽很久？
4. 你有没有做过专门的保护措施或演练？
5. 如果现在还没做，你打算怎么补最划算？


---
### 30. 你说你的防脏写是通过 Edit Lock + 心跳续期来做的。那我继续追问：如果用户拿到锁后一直挂机不动，别人又真的急着要编辑，怎么办？

**连续追问：**
1. 你的锁是绝对排他，还是允许管理员强制接管？
2. 如果允许接管，被接管的那个人本地还在编辑怎么办？
3. 你有没有给前端做“锁快过期 / 锁已被抢占”的实时提示？
4. 如果候选人只回答“有 TTL 自动释放”，我会继续问：那 TTL 内别人是不是只能干等？
5. 如果面试官进一步追问：这是不是说明你这个方案更像‘防乱写’，而不是‘高质量协同’？你怎么回答？


---

### 31. 你说 MySQL 和 ES 最终一致。那我继续追问：如果用户刚上传完图片，在列表页能看到，但搜索里搜不到，这算不算 bug？

**连续追问：**
1. 你会把它定义成“最终一致窗口”，还是“功能缺陷”？为什么？
2. 如果用户无法接受这个窗口，你会怎么优化？
3. 你有没有在前端明确提示“处理中 / 索引构建中”？
4. 如果 ES 长时间追不上 MySQL，这时候问题就不只是窗口了，你怎么判定已经异常？
5. 如果候选人回答得太理想化，我会继续问：实验室项目里你真的做了这些状态流转和异常提示吗，还是只是设计上会这么做？


---

### 32. 你说把 eo-learn 的 EOTask 封成原子工具给大模型调。那我继续追问：模型真的知道什么时候该调 NDVI、什么时候该调云检测吗？

**连续追问：**
1. 你的工具 description 里是怎么告诉模型使用边界的？
2. 如果模型对遥感领域理解不够，它会不会选错工具？
3. 你有没有加规则兜底，比如某些输入类型只能调用某些工具？
4. 如果模型调错了工具，后续结果是不是会层层污染，最后报告看起来很专业但其实方向错了？
5. 如果候选人回答“主要靠 prompt 引导”，我会继续问：那这是不是说明这个系统的可靠性其实很脆？


---
